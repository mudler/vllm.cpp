# CLAIM-BACKEND-TENSTORRENT-KEEPQUANT

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-BACKEND-TENSTORRENT-KEEPQUANT` | `BACKEND-TENSTORRENT-KEEPQUANT` (`ACTIVE`, new) | Maki (zai-glm-5.3-flash), helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/tenstorrent-keepquant.md` | isolated worktree `/tmp/row-tt-keepquant`; local Blackhole P150 under the `${GPU_LOCK:-$HOME/gpu.lock}` file mutex (not an `rc` fleet device) | `row/BACKEND-TENSTORRENT-KEEPQUANT`, base `origin/main` `bcbfee7bf`, issue [#2959](https://github.com/mudler/vllm.cpp/issues/2959) | Owns ONLY: the dense keep-quant dot on TT-Metal (Q4_K first, then Q5_K/Q6_K/Q8_0), the on-core block-decode path feeding the existing bf16 tile matmul with blocks resident in DRAM, the `kTENSTORRENT` arm of `DeviceKeepQuantSupported` narrowed to exactly the registered kernels, the provider registration, the capture-safe residency of that path, the quantized-Qwen3.5 vehicle gate and its `docs/USAGE.md` pin, and this row's records. EXCLUDES: the MoE grouped keep-quant GEMM (follow-up row), the block-decoding n-gram gather (#2394), ROCm keep-quant gaps (#2109, #1876, #1940), the IQ-family / sub-IQ1_S encodings, and the int8-dot perf lever (named next lever, not this row's correctness bar) | `ACTIVE` | 2026-09-05 — W0 staged: substrate surveyed (tt-metal has no packed-weight matmul; ttnn quantization is per-tensor activation dquant only), design settled (resident blocks, on-core decode, bf16 tile dot, decode bit-exact vs `BlockToFloat`, dot gated at the device-bf16 band), predicate seam named (`gguf_keep_quant.cpp:136-148`), vehicle and target artifact named with the 27B pin already in `docs/USAGE.md:914-915` |
