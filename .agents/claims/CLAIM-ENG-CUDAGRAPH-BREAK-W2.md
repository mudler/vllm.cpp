# CLAIM-ENG-CUDAGRAPH-BREAK-W2

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-CUDAGRAPH-BREAK-W2` | `ENG-CUDAGRAPH-BREAK` (`ACTIVE`) | Claude Code (opus-5), fresh implementer | isolated worktree; NO GPU lease obtained (`dgx:gpu0` held by another session for the whole window, `orin:gpu0` cannot run a CUDA 13 runtime), no registry, no publication | `row/ENG-CUDAGRAPH-BREAK-W2`, issue [#1261](https://github.com/mudler/vllm.cpp/issues/1261), parent [#1163](https://github.com/mudler/vllm.cpp/issues/1163), predecessor [#1192](https://github.com/mudler/vllm.cpp/issues/1192) | Owns ONLY W2 of [`eng-cudagraph-break.md`](../specs/eng-cudagraph-break.md): migrating `Qwen3DenseDecodeGraph` (`src/vllm/model_executor/models/qwen3.cpp`) onto `vt::GraphCaptureScope` + `vt::BreakableGraph`, the `vt::GraphCaptureMode` addition to the seam (`include/vt/breakable_graph.h`, `src/vt/breakable_graph.cpp`) that mirrors vLLM's FULL/PIECEWISE split, the new G2 gate `tests/vllm/models/test_qwen3_decode_graph_seam.cpp` and its CMake registration, and the three new mode cases in `tests/vt/test_breakable_graph.cpp`. EXCLUDES the other eight drivers (W3 through W5), the persistent DEVICE input path and the async decline it would retire (W4), the `pure_decode` predicate at `src/vllm/v1/worker/gpu/runner.cpp:1341` (W6), the auxiliary-stream auto-join (D10, W4/W5), and graph-executable dedup, which is [#1162](https://github.com/mudler/vllm.cpp/issues/1162) on a parallel branch and whose files this claim never touches | `ACTIVE` | 2026-08-18 — the seam is ENTERED from a production step and W1's staged slice is retired. Focused green: `test_qwen3_decode_graph_seam` 3/3 (124 assertions), `test_breakable_graph` 27/27 (190), `test_qwen3_break_point` 2/2 (516), `test_qwen3_forward` 10/10 (1575), `test_decode_graph_sizes` (478). Three mutations, each printed with its diff, compile status and exit status, each detected and each restored byte-for-byte. **G1 NOT met and recorded as owed**, not implied: no lease was obtainable and a CPU harness cannot replay a captured segment. W3 through W6 unclaimed |
