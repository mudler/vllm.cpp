# CLAIM-ENG-CUDAGRAPH-BREAK-W3

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-CUDAGRAPH-BREAK-W3` | `ENG-CUDAGRAPH-BREAK` (`ACTIVE`) | Claude Code (opus-5), fresh implementer | isolated worktree; one `rc` lease on `thor:gpu0` (NVIDIA Thor, sm_110, driver 595.78) for G1; no registry, no publication | `row/ENG-CUDAGRAPH-BREAK-W3`, issue [#1291](https://github.com/mudler/vllm.cpp/issues/1291), parent [#1163](https://github.com/mudler/vllm.cpp/issues/1163), predecessors [#1192](https://github.com/mudler/vllm.cpp/issues/1192) (W1) and [#1261](https://github.com/mudler/vllm.cpp/issues/1261) (W2) | Owns ONLY W3 of [`eng-cudagraph-break.md`](../specs/eng-cudagraph-break.md): migrating `Qwen3MoeDecodeGraph` (`src/vllm/model_executor/models/qwen3_moe.cpp`), `VoxtralDecodeGraph` (`voxtral.cpp`) and `DeepseekV2DecodeGraph` (`deepseek_v2.cpp`) onto `vt::GraphCaptureScope` + `vt::BreakableGraph` in `kFull`; the `full_scopes`/`piecewise_scopes` counters added to `vt::GraphBreakStats` (`include/vt/breakable_graph.h`, `src/vt/breakable_graph.cpp`) because a W3 mutation measured the existing mode guard to be a mute switch; the shared gate harness `tests/vllm/models/decode_graph_seam_harness.h` and W2's gate moving onto it; the three per-driver G2 gates; the G1 gate `tests/vllm/models/test_decode_graph_seam_g1_cuda.cpp` and its lease run; and their CMake registration. EXCLUDES the two Qwen3.5 drivers and the persistent DEVICE input path (W4), the three single-shape drivers (W5), the `pure_decode` predicate at `src/vllm/v1/worker/gpu/runner.cpp:1341` (W6), the auxiliary-stream auto-join (D10, W4/W5), the missing `device_token_ids` decline on the Qwen3-Coder and DeepSeek registrations ([#1305](https://github.com/mudler/vllm.cpp/issues/1305), filed here, owner W4), and graph-executable dedup ([#1162](https://github.com/mudler/vllm.cpp/issues/1162), a parallel branch whose files this claim never touches) | `ACTIVE` | 2026-08-19 (fresh implementer) — three drivers migrated, one commit each, each with a RED-first G2 gate; G1 delivered on a leased `thor:gpu0`, retiring the item W1 and W2 both carried as owed. Two findings the stage did not go looking for: the `breaks_registered == 0` mode guard is a TAUTOLOGY for every model with no registered break point (a one-token `kFull` -> `kPiecewise` flip left a whole gate green at 226/226), closed by counting the mode itself; and three decode-graph registrations route asynchronous steps into a host-vector replay with no `device_token_ids` decline, filed as #1305 with W4 as owner rather than fixed in flow |
