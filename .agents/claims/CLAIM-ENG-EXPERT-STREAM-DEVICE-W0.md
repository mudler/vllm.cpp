# CLAIM-ENG-EXPERT-STREAM-DEVICE-W0

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-EXPERT-STREAM-DEVICE-W0` | `ENG-EXPERT-STREAM-DEVICE` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec `.agents/specs/expert-stream-device-slots.md` | isolated worktree, CPU only. `dgx:gpu0` was NOT used: W0a (the `cudaDevAttrPageableMemoryAccess` probe) was submitted to `rc` FIRST, before any tree change, and is still QUEUED behind a four-hour job, so every GPU-dependent result is recorded as `PENDING` rather than claimed | `row/ENG-EXPERT-STREAM-DEVICE-W0`, issue [#1124](https://github.com/mudler/vllm.cpp/issues/1124) | Owns ONLY W0b, W0c and W0d of the committed spec: the NEW `Platform::host_memory_is_device_addressable()` and its CUDA and ROCm legs plus the new HIP-free `vt::rocm::HostMemoryIsDeviceAddressable` probe; the platform branch, the `KqHostSliceView` helper and the streamed-tower refusal in `qwen3_5.cpp`, with the `expert_streamed` marker on `OwnedTensor` and two `detail::` test hooks; the `StreamedExpertLane` input and `GgufLargestExpertSliceBytes` in the GGUF fit bound plus the loader block that fills it; the row's state move to `ACTIVE` with its two summary counts; and the four gate files. EXCLUDES the streaming MECHANISM (`ENG-EXPERT-STREAM` — the cache, streamer, `pread` filler and host store are untouched); W1's `DeviceExpertSlotStore` and its `CommitSlot` fill contract; W2's virtual `SlotForRead`; `.agents/specs/expert-streaming.md`, deliberately, because PRs #1200 and #1216 both edit it; the KV/activation term of the fit bound, which `KV-WARMUP-PROFILE` owns; and the block-wise FP8 dense-loader refusal that [#1240](https://github.com/mudler/vllm.cpp/issues/1240) records against `MODEL-FP8-BLOCK-WEIGHT` | `ACTIVE` | 2026-08-18 — W0b/W0c/W0d landed spec-first and unit-gated (13/13, 13/13, 8/8, 5/5 cases), ten mutations red with compile status and non-zero case counts printed, full build 836/836 clean, ctest 523/525 with both failures attributed to `origin/main`. W0a and W0e remain queued on `dgx:gpu0` and no GPU number is claimed |
