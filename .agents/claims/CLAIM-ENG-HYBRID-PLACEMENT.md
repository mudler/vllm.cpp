# CLAIM-ENG-HYBRID-PLACEMENT

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-HYBRID-PLACEMENT` | `ENG-HYBRID-PLACEMENT` (`ACTIVE`) | Claude Code (opus-5), operator role — coordinating the campaign; each wave goes to a fresh implementer and a fresh reviewer | isolated worktree `/home/mudler/_git/vllm.cpp-hybspec`, CPU only. NO GPU, NO lease, NO checkpoint, NO benchmark. The llama.cpp anchors were read with `git show b10451:<path>` against the local `/home/mudler/_git/llama.cpp` checkout, never from its working tree, because that tree sits at the superseded `237ad9b96` | `row/ENG-HYBRID-PLACEMENT-SPEC-V2`, PR [#2016](https://github.com/mudler/vllm.cpp/pull/2016), issue [#2015](https://github.com/mudler/vllm.cpp/issues/2015) under campaign [#149](https://github.com/mudler/vllm.cpp/issues/149) | This claim owns ONLY the spec revision and the row's records: `.agents/specs/hybrid-placement.md`, the `ENG-HYBRID-PLACEMENT` row in `.agents/engine-matrix.md`, this claim file, and the `#2015` row in `.agents/issue-index.md`. EXCLUDES all product code, which W1-W4 owe. EXCLUDES the `qwen4_exp` rows in `.agents/model-matrix.md` and `docs/FEATURES.md`, which `CLAIM-MODEL-MM-QWEN4-EXP-W1` holds. EXCLUDES the `docs/FEATURES.md` placement row, which PR [#2014](https://github.com/mudler/vllm.cpp/pull/2014) carries under issue [#2012](https://github.com/mudler/vllm.cpp/issues/2012) | `ACTIVE` | 2026-08-26 — row claimed and moved `READY`→`ACTIVE`. The spec is re-anchored at the recorded pin `b10451` (the previous `237ad9b96` citations do not resolve there), gains `## Configuration surface` mapping all four llama.cpp placement surfaces onto `--offload-config`'s `vllm_cpp` key per developer direction, and renumbers `## Work breakdown` so the four hardware-independent waves precede the two that need a discrete-GPU rig. **W5 (speed floor vs `-ncmoe`) and W0 (the bandwidth and round-trip measurements) are BLOCKED and listed under `## Owed`**: `rc devices` on 2026-08-26 lists `dgx:gpu0`, `orin:gpu0` and `thor:gpu0`, all integrated parts where this capability is inert by construction. No number is claimed here and nothing has been benchmarked |
