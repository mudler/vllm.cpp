# CLAIM-ENG-NOW-DERIVED

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-NOW-DERIVED` | `ENG-NOW-DERIVED` (`ACTIVE`) | Claude Code (opus-5), helper role | isolated worktree `/home/mudler/_git/vllm.cpp-now-derived`; no GPU, no registry, no publication | `row/ENG-NOW-DERIVED`, base `origin/main` `f64f2b71`, issue [#374](https://github.com/mudler/vllm.cpp/issues/374) | Owns ONLY: NEW `.agents/specs/now-derived.md`; NEW `scripts/now.py` + `tests/scripts/test_now_render.py`; the `NOW` removal from `check-doc-checkpoint.py`'s lifecycle triple and its `spec_now_errors` replacement + `tests/scripts/test_doc_checkpoint.py`; the `ROW_TABLE_LINE` regrowth guard in `check-now-current.py` + its test; the live-claims table in `.agents/NOW.md`; and the §Records compression + cold-start step in `AGENTS.md`. EXCLUDES the legacy claims table in `coordination.md` (emptying on its own since #364), every other public document, and all product source | `ACTIVE` | 2026-08-11 — spec committed; W1-W5 implemented; `test_doc_checkpoint` 26, `test_now_render` 11, `test_check_now_current` 15 green, each with the mutation that disarms its guard |
