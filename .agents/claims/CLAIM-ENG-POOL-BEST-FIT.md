# CLAIM-ENG-POOL-BEST-FIT

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-POOL-BEST-FIT` | `ENG-POOL-BEST-FIT` (`ACTIVE`, new) | Claude Code (opus-5), fresh implementer — review goes to a different agent | isolated worktree `.claude/worktrees/agent-a987a48a38d1af0f5`; NO GPU and no `rc` lease (`dgx:gpu0` held by the developer for the #1574 benchmark round), CPU build only | `row/ENG-POOL-BEST-FIT`, base `origin/main` `a73b26968`, issue [#1922](https://github.com/mudler/vllm.cpp/issues/1922), also filing [#1926](https://github.com/mudler/vllm.cpp/issues/1926) and [#1927](https://github.com/mudler/vllm.cpp/issues/1927) | Owns ONLY: `.agents/specs/pool-best-fit-retention.md`; the bounded best-fit borrow and the `stats()` accessor in `include/vllm/model_executor/models/device_pool.h`; `tests/vllm/v1/worker/test_engine_scratch_steady_state.cpp` and its CMake registration; the `VT_POOL_BORROW` row in `docs/ENVIRONMENT.md` and the pool paragraph in `docs/reference/engine-lifecycle.md`; the `ENG-POOL-BEST-FIT` engine-matrix row and the three issue-index rows. EXCLUDES the DFlash2 draft store and `src/vllm/model_executor/models/qwen3_dflash.cpp`, which [#1919](https://github.com/mudler/vllm.cpp/issues/1919)'s implementer holds; EXCLUDES `src/vllm/v1/worker/gpu/runner.cpp`, so [#1927](https://github.com/mudler/vllm.cpp/issues/1927) is filed and owed rather than fixed here; EXCLUDES the other shape-keyed caches ([#1926](https://github.com/mudler/vllm.cpp/issues/1926)); EXCLUDES setting `device_pool_cap_bytes` on any platform, which the pool header records as owing its own `PreGrowForCapture` repair | `ACTIVE` | 2026-08-25 — PR [#1930](https://github.com/mudler/vllm.cpp/pull/1930) open, body verified by `scripts/agent-pr-body.py --pr 1930` (exit 0). Spec committed spec-first with its records. Focused gate `test_engine_scratch_steady_state` 12/12 green; the same binary under `VT_POOL_BORROW=0` is 6/12 `FAILURE` and prints the pre-fix curve (retained 560 243 B → 2 867 707 B over twelve DESCENDING requests, 240 driver allocations for eleven requests that each demanded strictly less than one already served). Three mutations applied, each detected, each restored to sha256 `5ac6b98…`: the borrow loop disabled (both cases red, which is also the reachability proof because the engine case enters only through `LoadedEngine::generate`), a borrowed block demoted on return (unit case red), and the instrument returning a constant (the gate REFUSES rather than passing). **THIS ROW DOES NOT CLOSE #1922**: no GPU was available, so #1922's own `avail` curve at `--max-model-len 12288` on `dgx:gpu0` is neither reproduced nor attributed, and that run is `## Owed` O1 — operator-run |
