# CLAIM-ENG-PREFLIGHT-COMPILES

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-PREFLIGHT-COMPILES` | `ENG-PREFLIGHT-COMPILES` (`ACTIVE`) | Claude Code (opus-5), fresh implementer — review goes to a different agent | linked worktree `/home/mudler/_git/vllm.cpp-preflight-compiles`; CPU only, no GPU lease taken or needed | `row/ENG-PREFLIGHT-COMPILES`, issue [#2401](https://github.com/mudler/vllm.cpp/issues/2401) | Owns ONLY: `.agents/specs/preflight-compiles.md`; `scripts/check-tree-compiles.py`; `tests/scripts/test_check_tree_compiles.py`; the one compile block added to `scripts/agent-preflight.sh` and its `SUITES` entry; the CI script-lane registration; the `ENG-PREFLIGHT-COMPILES` cell in `.agents/engine-matrix.md` and its two count cells. EXCLUDES every edit to `AGENTS.md`, which this row deliberately does not make and proposes instead; EXCLUDES every existing checker's semantics, the `CHECKERS` array's membership, `.githooks/pre-push`, every CI build job, and any change to what preflight demands of a `read-only` session | `ACTIVE` | 2026-08-31 — spec committed ahead of the implementation. Red-before targets pinned: `76fefdca7..77cedc9a5` (`5263ac31f^`) and `bcadc3a64..c27246d37` (`08fa2f5aa^`), two trees that pass every existing gate |
