# CLAIM-ENG-RECORD-CONFLICT-SURFACES

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-RECORD-CONFLICT-SURFACES` | `ENG-RECORD-CONFLICT-SURFACES` (`ACTIVE`, was `READY`) | Claude (claude-opus-5), operating from the committed spec `.agents/specs/derived-issue-index.md` | isolated worktree `.wt/POLICY-DERIVED-INDEX`; no GPU, no lease — this row is local protocol machinery | `row/POLICY-DERIVED-INDEX`, base `origin/main` `e541be98`, issues [#364](https://github.com/mudler/vllm.cpp/issues/364), [#2290](https://github.com/mudler/vllm.cpp/issues/2290), [#883](https://github.com/mudler/vllm.cpp/issues/883), [#2157](https://github.com/mudler/vllm.cpp/issues/2157), [#467](https://github.com/mudler/vllm.cpp/issues/467), [#1808](https://github.com/mudler/vllm.cpp/issues/1808), [#2298](https://github.com/mudler/vllm.cpp/issues/2298) | Owns ONLY: `.agents/issue-index.md` and its `merge=union` attribute (retired to `.agents/completed/`), `scripts/agent-issue-index.py` (new), `check_issue_index` in `check-agent-record.py`, the index reads in `check-pr-size.py` and `check-symbol-anchors.py`, `check-issue-index-append-only.py` (deleted with its suite), the merge-commit rule in `check-commit-trailers.py --range`, `agent-preflight.sh`'s checker discovery, and four `AGENTS.md` sections — §"Every change starts from an issue", §Records, §"Landing work", §"How work gets done". EXCLUDES: every other record surface, every public document, `agent-pr-body.py`'s contract (kept, not changed), and all product source — no kernel, model, backend or gate semantic outside those checkers moves | `ACTIVE` | 2026-08-29 — W6-W8 on branch. W6 derives the index from `gh issue list` into an untracked snapshot and retires the `merge=union` record shape AGENTS.md admitted but GitHub never implemented; the one-time backfill wrote `Row:` into 369 issue bodies, verbatim-preserving each. W7 gives the trailer walk the merge-commit rule `ci.yml:872` already had (#2157). W8 takes preflight from 5 to 42 checkers, measured at 27s total. Red-first throughout: 8 of 11 rewritten index cases, both merge-commit cases, and the merge-shape regression's own control |
