# CLAIM-ENG-RESIDENCY-CONFIG

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-ENG-RESIDENCY-CONFIG` | `ENG-RESIDENCY-CONFIG` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec | isolated worktree, CPU only; no GPU, no oracle run, no publication. dgx.casa was NOT used and is unreachable at the SSH layer (TCP/22 accepts, then times out during banner exchange), so the measured 370 GiB reproduction is recorded under `## Owed` rather than claimed | `row/ENG-RESIDENCY-CONFIG`, issue [#1110](https://github.com/mudler/vllm.cpp/issues/1110) | Owns ONLY: NEW `.agents/specs/weight-residency-config.md`; the NEW `ENG-RESIDENCY-CONFIG` row plus the two summary counts and the `ENGINE_ROWS` mark it moves; NEW `include/vllm/config/weight_residency.h` + `src/vllm/config/weight_residency.cpp`; the `weight_residency` field on `EngineParams`; the install block in `LoadedEngine::FromModelDir`; the extension parse in `server_main.cpp` and `src/capi/vllm_c.cpp`; the three resolve sites (`GgufLoadPolicy::FromEnv`, `PrefaultBorrowedSpan`, `Qwen35ExpertStreamRequested` + the `Qwen35ExpertStream` constructor); its three RED-first suites; and the `docs/USAGE.md` / `docs/ENVIRONMENT.md` entries including the [#1109](https://github.com/mudler/vllm.cpp/issues/1109) default correction. EXCLUDES every field of `OffloadConfig`, `UVAOffloadConfig`, `PrefetchOffloadConfig` and their validator — the mirror of `vllm/config/offload.py` stays byte-faithful and a test asserts it; the streaming MECHANISM (`ENG-EXPERT-STREAM`); the device→host tier (`ENG-WEIGHT-OFFLOAD`); `VT_MOE_EXPERT_STREAM_STATS_EVERY`, which stays environment-only by decision; and the rest of the load-transform family (`VT_GGUF_KEEP_QUANT`, `VT_CPU_REF`, `VT_GGUF_KEEP_F16`, …), which is a different tier | `ACTIVE` | 2026-08-17 — spec committed first, implementation and its three suites landed, reachability proven by deleting the install call site |
