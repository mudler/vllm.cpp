# CLAIM-GLM53-FLASH-ROCM

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-GLM53-FLASH-ROCM` | `MODEL-MM-glm5-next-glm5-next-for-conditional-generation` (`ACTIVE`) | fresh implementer, helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/glm5-next-flash.md` §W10 | isolated worktree `/home/mudler/_git/vllm.cpp-glmrocm`; device `strix:gpu0` (`gfx1151`, AMD Ryzen AI MAX+ 395 w/ Radeon 8060S, ROCm 7.2.4) taken through `rc run`, never `ssh` | `row/MODEL-MM-GLM53-FLASH-ROCM`, base `origin/main` `263f6d50d`, issue [#2462](https://github.com/mudler/vllm.cpp/issues/2462) | Owns ONLY: §W10 (the ROCm arm-by-arm map and its device-fit measurement), `## Owed` O40/O42, the IQ2_XS/IQ4_XS residual in O41, the `## Now` head, `.agents/scripts/glm53-rocm-memfit.{hip,sh}`, and this claim file. EXCLUDES: the CUDA device arm (`glm5_next_*`, `cuda_glm5_next.cu`, W9b/W9c-1..3 — another wave owns those), every `src/vt/rocm/*.hip`, `gguf_keep_quant.cpp`, and any ROCm MLA/k-pool/quant kernel; the GFX1100 reconstruction changes the keep-quant product files outside this claim | `ACTIVE` | 2026-09-09 — The 58.000 GiB measured device ceiling remains below the 101.2535 GiB packed artifact, so device fit remains blocked by at least 1.75x. The GFX1100 reconstruction admits 11 ROCm formats and now covers IQ3_XXS/Q2_K/Q3_K from this artifact. IQ2_XS and IQ4_XS still expand. The exact mixed-residency footprint is `PENDING` an artifact rerun; the former 10.1x value is obsolete |
