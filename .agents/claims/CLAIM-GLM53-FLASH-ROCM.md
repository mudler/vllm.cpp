# CLAIM-GLM53-FLASH-ROCM

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-GLM53-FLASH-ROCM` | `MODEL-MM-glm5-next-glm5-next-for-conditional-generation` (`ACTIVE`) | fresh implementer, helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/glm5-next-flash.md` §W10 | isolated worktree `/home/mudler/_git/vllm.cpp-glmrocm`; device `strix:gpu0` (`gfx1151`, AMD Ryzen AI MAX+ 395 w/ Radeon 8060S, ROCm 7.2.4) taken through `rc run`, never `ssh` | `row/MODEL-MM-GLM53-FLASH-ROCM`, base `origin/main` `263f6d50d`, issue [#2462](https://github.com/mudler/vllm.cpp/issues/2462) | Owns ONLY: §W10 (the ROCm arm-by-arm map and its device-fit measurement), `## Owed` O40/O41/O42, the `## Now` head, `.agents/scripts/glm53-rocm-memfit.{hip,sh}`, and this claim file. EXCLUDES: the CUDA device arm (`glm5_next_*`, `cuda_glm5_next.cu`, W9b/W9c-1..3 — another wave owns those), every `src/vt/rocm/*.hip` (untouched), `gguf_keep_quant.cpp` (measured and deliberately NOT widened, O41), and any ROCm MLA/k-pool/i-quant kernel (priced in §W10, not written) | `ACTIVE` | 2026-09-01 — W10 lands the map and the refusal. `strix:gpu0` holds at most **58.000 GiB** of `hipMallocManaged` (three samples: jobs `e87ec9b6` and `625084ba` at 2 GiB granularity, `08b6baee` at 4 GiB reading 56.000; run 3 executed the committed script verbatim) against an artifact of **101.2535 GiB**, so the model does not fit in the BEST case (1.75x over) let alone today's (10.1x). No ROCm kernel written, because one would land dead. The one cheap slice — widening `DeviceKeepQuantSupported`'s ROCm arm — was measured against `rocm_grouped_gemm.hip` and REFUSED: the predicate is already exactly as wide as the kernel |
