# CLAIM-GLM53-FLASH-W9C3A

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-GLM53-FLASH-W9C3A` | `MODEL-MM-glm5-next-glm5-next-for-conditional-generation` (`ACTIVE`) | fresh implementer, helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/glm5-next-flash.md` §W9c-3a | isolated worktree `/home/mudler/_git/vllm.cpp-glmfwd2`; host gates on x86_64, device leg on `dgx:gpu0` (GB10, `sm_121a`) taken through `rc run` as job `0b0d9c90-0b6d-488e-afb5-076d3d018ed6`, never `ssh` | `row/MODEL-MM-GLM53-FLASH-W9C`, base `origin/main` `839ea1ced`, issue [#2464](https://github.com/mudler/vllm.cpp/issues/2464), PR [#2468](https://github.com/mudler/vllm.cpp/pull/2468) | Owns ONLY: §W9c-3a, the narrowing of `## Owed` O19, `## Owed` O43/O44/O45, the `## Now` head, the routed-expert device arm in `glm5_next_moe.{h,cpp}`, the three source pointers in `glm5_next_bridge.cpp`, the `dev` parameter threaded through `glm5_next_layer.{h,cpp}`, the queue split in `glm5_next_forward.cpp`, the two glm5_next test suites, the two `docs/` clauses this change falsifies, and this claim file. EXCLUDES: every CUDA kernel (`cuda_quant_dot.cu` is read and cited, never edited — #2260 owns it), `glm5_next_attn.cpp` / `glm5_next_dsa.cpp` / `glm5_next_kda.cpp` / `glm5_next_mhc.cpp` (W9c-1 and W9c-2 own those), the ROCm arm (W10's, and its O40/O41/O42 are untouched), `dense_attn_block.h` and `dense_device_glue.h` (consumed, not modified), and the k-pool device ops (W9c-0's, still unreached per O36) | `ACTIVE` | 2026-09-01 — W9c-3a puts the routed-expert keep-quant GEMM on the device and stops the forward refusing a CUDA queue. It exists because **O19's kernel blocker was stale**: `IsCudaKeepQuantSupported` has admitted `IQ2_XS` and `IQ4_XS` since #2260, which names this artifact's own 82 + 3 tensors. ONE arm of eleven moved; O43 names the other ten. Gated on x86_64 at 18/12731, 28/200, 10/1656 and 20/32562 with five mutations, four killed by assertion and the fifth (the layer's `dev` hand-off) only by `-Werror=unused-parameter`, recorded as the weaker result it is. **NO TOKEN HAS COME OUT OF THIS MODEL ON A GPU** and none is claimed (O45): the end-to-end `--device cuda` leg is queued on `dgx:gpu0` and an untaken device gate is PENDING, never a pass |
