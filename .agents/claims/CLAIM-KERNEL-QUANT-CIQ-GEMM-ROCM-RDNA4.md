# CLAIM-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4` | `KERNEL-QUANT-CIQ-GEMM-ROCM` (`ACTIVE`) | Claude Code (sonnet-5) coordinating; implementation, review, repair, and re-review each ran as separate agents per `AGENTS.md` "How work gets done" | local worktree `/home/justin/Projects/vllm.cpp-KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1`, RX 9060 XT (gfx1200), not a fleet device — flock `$HOME/gpu.lock` for every GPU-touching command | `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1` (branched from `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4`'s spec-only commit); issue [#2109](https://github.com/mudler/vllm.cpp/issues/2109) | Owns: `KQuantGemmKWmmaQ6K`, `KQuantGemmKWmmaQ4K`, `DequantQ6KGroup16`, `DequantQ4KTile16`, `UnpackQ4KScalesMins`, `Gfx12QuantWmmaHostOk`, `QuantWmmaEnabled`, `kQuantWmmaWarpsPerBlock`/`kGroupGemmBatch`, and the dispatch-count witnesses, all in `src/vt/rocm/rocm_grouped_gemm.hip`; the `VT_ROCM_QUANT_WMMA` env toggle and its `docs/ENVIRONMENT.md` row; the new `TEST_CASE`s in `tests/vt/test_backend_cross_device.cpp`; the device path and tail-shape bench case added to `examples/quant_gemm_bench/main.cpp`; this row's spec and its `.agents/kernel-matrix.md` cell; and the `KERNEL` row-count bump in `scripts/check-agent-record.py`. EXCLUDES the existing scalar `KQuantGemmK`/`Dp4a`/`DotQ4K`/`DotQ6K` bodies (untouched), the Q8_K activation quantizer and its arch-selector machinery (`Q8KArchCache`, `SelectQ8KQuantArm`, untouched), the cooperative Q6_K decode arm `KQuantGemmKCoopQ6K` (untouched), and every non-gfx1200/gfx1201 ROCm target's dispatch (unaffected by construction — the host gate never selects this arm there) | `ACTIVE` | 2026-09-05 — W0-W2 landed as ten commits on `row/KERNEL-QUANT-CIQ-GEMM-ROCM-RDNA4-w1`: the Q6_K WMMA tile, two performance passes on it, the Q4_K WMMA tile, a run of the issue's own gate (c) recipe against the pinned `b10451` oracle (narrowly satisfied against its stated ≤20,000ms threshold; oracle-side absolute-figure reconciliation stays open, recorded rather than elided), and the M/N tail fix (a real prompt is essentially never 16-aligned, so this closes the case that mattered most). **Two independent review rounds, PASS both times.** Round 1 (whole wave): PASS, two LOW findings (a stale doc row; the tail-fill launch sized to the full `m*n` grid rather than the remainder). A fresh implementer repaired both — the second repair also produced a measured, previously-unmeasured perf win (+15.8-20.9% on wide-N tail shapes) neither finding asked for. Round 2 (scoped to the repair): PASS, independently reproduced every mutation and benchmark claim with its own fresh mutations and a same-tool same-binary swap, one further cosmetic finding folded into the repair commit directly. `KQuantGemmK` gained a `skip_m`/`skip_n`-driven index remap (not a skip-guard — that mechanism was retired in the same repair) so the tail-fill launch enumerates only the uncomputed remainder. PRs opened and marked ready for review: spec-only [#2990](https://github.com/mudler/vllm.cpp/pull/2990), implementation [#2991](https://github.com/mudler/vllm.cpp/pull/2991) (carries the closing keyword for #2109). Q5_K and the gfx1100/gfx1151 WMMA tiles remain `## Owed`, each a separate row. Remaining: merge, which needs the developer's go-ahead per `AGENTS.md` "Landing work" |
