# CLAIM-LTX25-DEVICE-RESIDENCY-W5

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-LTX25-DEVICE-RESIDENCY-W5` | `KERNEL-CONV3D` (`ACTIVE`), `LTX25-DEVICE-RESIDENCY` (`ACTIVE`, W5 only) | Claude Code (opus-5), fresh implementer | isolated worktree; **NO GPU lease** — the row was dispatched while a render held `dgx:gpu0` and the lease was explicitly withheld, so nothing here is measured on an accelerator | `row/LTX25-DEVICE-RESIDENCY-W5`, issue [#1007](https://github.com/mudler/vllm.cpp/issues/1007), parent [#1264](https://github.com/mudler/vllm.cpp/issues/1264) | Owns ONLY W5 of [`ltx25-device-residency.md`](../specs/ltx25-device-residency.md): the new `vt::OpId::kConv3d` with its `Conv3dArgs` contract, validating wrapper, `src/vt/cpu/cpu_conv3d.cpp`, `src/vt/cuda/cuda_conv3d.cu`, `tests/vt/test_ops_conv3d.cpp`, the CONVOLUTION dispatch inside `CausalConv3d` in `src/vllm/model_executor/models/ltx2_video_vae.cpp`, the queue parameter threaded through `ltx2_video_vae_tiled.cpp` and the `decode.video` call site in `src/vllm/multimodal/ltx2_video.cpp`, and one case each in `tests/vllm/models/test_ltx2_vae.cpp` and `tests/vllm/multimodal/test_diffusion_device_seam.cpp`. EXCLUDES `kConv2d` and every other convolution op, which keep their own accumulator orders; the four other host conv3d loops (`minimax_h3_vae_cnn.cpp`, `minimax_h3_video_vae.cpp`, `ltx2_upsampler.cpp`, `clip_mmproj_gguf.cpp`), deliberately not rewired; the decode's non-convolution stages, which are [#1451](https://github.com/mudler/vllm.cpp/issues/1451); the CUDA arm's first execution, which is [#1452](https://github.com/mudler/vllm.cpp/issues/1452); the memory format [#1011](https://github.com/mudler/vllm.cpp/issues/1011); and W1 through W4 and W6/W7 of the campaign | `ACTIVE` | 2026-08-20 — CPU tier landed and byte-identity gated, REACHED from the render path (deleting the dispatch reds 12 of `test_ltx2_vae`'s 44 cases), and the device MARSHALLING executed against a fake unified-memory backend with `memcmp`-identical pixels. **NO speed claim, no ratio, no lease.** The CUDA arm is registered and has never been compiled or executed |
