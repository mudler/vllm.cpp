# CLAIM-LTX25-VAE-DEVICE-RESIDENCY

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-LTX25-VAE-DEVICE-RESIDENCY` | `KERNEL-LTX2-VAE` (`ACTIVE`) | Claude Code (opus-5), fresh implementer — the review went to a different agent and its findings are repaired here | isolated worktree `/home/mudler/_git/wt-1451`; **no GPU lease taken or authorised**, CPU build only | `row/LTX25-VAE-DEVICE-RESIDENCY`, issue [#1451](https://github.com/mudler/vllm.cpp/issues/1451), PR [#1909](https://github.com/mudler/vllm.cpp/pull/1909) | Owns ONLY: `.agents/specs/ltx25-vae-device-residency.md`; `vt::OpId::kLtx2Vae` and its ten-entry table (`include/vllm/model_executor/models/ltx2_video_vae_kernels.h`, `src/vt/cpu/cpu_ltx2_vae.cpp`, `src/vt/cuda/cuda_ltx2_vae.cu`, `src/vllm/model_executor/models/ltx2_video_vae_device_resolve.cpp`); the residency plumbing in `src/vllm/model_executor/models/ltx2_video_vae.cpp` (`VaeStore`, `VaeWeightCache`, `VaeScratch`, and the deletion of W5's `DevBuf`); the two LTX-2.5 VAE cases and the transfer counters in `tests/vllm/multimodal/test_diffusion_device_seam.cpp`; and the `KERNEL-LTX2-VAE` matrix row. EXCLUDES `vt::Conv3d`'s behaviour and the `KERNEL-CONV3D` row's contract, which stay frozen (only that row's stale `#1451` clause is corrected); EXCLUDES `AttnBlock3d`, which is the declared staged remainder and still runs on the host; EXCLUDES the video ENCODER, which stays host-only; and EXCLUDES [#1904](https://github.com/mudler/vllm.cpp/issues/1904)'s `DBuf` migration audit, which this row filed and did not take | `ACTIVE` | 2026-08-25 — PR [#1909](https://github.com/mudler/vllm.cpp/pull/1909) open, body verified by `scripts/agent-pr-body.py`. Red-first captured verbatim (7 downloads against the asserted 1). Ten CPU arms are the host loops transcribed, so the committed goldens gate the move: `test_ltx2_vae` 45/45 and 3152/3152 UNCHANGED from base, and a fresh review's twelve arithmetic mutations each red 2-8 golden cases. Residency asserted as a COUNT on a fake accelerator: exactly one host-device download, uploads equal to the distinct-tensor count, zero stage dispatches on `kCPU`. Two fixtures now reach **10 of 10** kernels on a non-CPU queue, measured by instrumenting each arm — the first draft reached four. Fresh review returned `FINDINGS` and they are repaired: six host pointers reaching device kernels (already fixed before the review landed, in `1beebf8fc`), the stale `KERNEL-CONV3D` clause, this missing matrix row and claim, three wrong `sampling.py` anchors, a dead `Ltx2VaeDeviceKernelsAvailable`, a bare `+=` breaking the CUDA file's own intrinsic discipline, and three overclaiming comments. **PENDING a lease and NOT claimed:** the CUDA arm has never been compiled or executed anywhere in this project's reach ([#1452](https://github.com/mudler/vllm.cpp/issues/1452)), and no speed or memory number was measured. Owed and recorded in the spec: nothing gates the queue on the production tiled chain; the f64 `group_norm` width is ungated; the weight cache's lifetime is one decode; the pad buffer is zero-filled twice on the CPU arm |
