# CLAIM-MODEL-DSV4-EXL3

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-MODEL-DSV4-EXL3` | `MODEL-DSV4-EXL3` (`ACTIVE`) | Claude Code (fable-5), helper role for the W1 fresh-review repair. **This cell records what `scripts/agent-role.py show` reports for the row (`role=helper row=MODEL-DSV4-EXL3 branch=row/MODEL-DSV4-EXL3`), not an operator identity — no operator claim for this row was recorded anywhere this file could read.** A coordinator holding the row from another checkout owns correcting the Agent and Worktree cells; nothing else in this row depends on them | worktree `/home/mudler/.cache/sdd/mudler-vllm.cpp/dsv4-exl3` (CPU-only build, `-DVLLM_CPP_CUDA=OFF -DVLLM_CPP_SERVER=OFF`); the shared checkout is untouched | `row/MODEL-DSV4-EXL3`, issue [#1875](https://github.com/mudler/vllm.cpp/issues/1875) | Owns: `.agents/specs/model-dsv4-exl3.md`; the `MODEL-DSV4-EXL3` row in `.agents/kernel-matrix.md`; `src/vt/cpu/cpu_exl3_dequant.cpp` + `tests/vt/test_exl3_dequant.cpp` (W1a); the EXL3 arm of `src/vllm/model_executor/models/deepseek_v4_weights.cpp` + `include/vllm/model_executor/models/deepseek_v4.h` + `tests/vllm/models/test_deepseek_v4_exl3_loader.cpp` (W1b). EXCLUDES: the `has_host_weights` guard in `deepseek_v4.cpp` (W1c owns making it name this row), every CUDA kernel (W2), `.agents/oracles/exllamav3.md` and any oracle-table edit (W3a), and `tests/CMakeLists.txt` (the surface [#1883](https://github.com/mudler/vllm.cpp/issues/1883) names, which no row owns) | `ACTIVE` | 2026-08-24 — W1a and W1b landed; the fresh review returned `PASS` with six MINOR findings and two NITs and the repair for all eight landed. This file exists because moving the matrix row from `READY` to `ACTIVE` (the review's F2) makes a `CLAIM-*` owner mechanically required by `scripts/check-agent-record.py:1785`, and the row had none. Next: W1c (dequant-to-bf16 fallback so the tower EXECUTES), then W2. |
