# CLAIM-QUANT-EXL3-MUL1

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-QUANT-EXL3-MUL1` | `QUANT-EXL3-MUL1` (`ACTIVE`) | Claude Code (opus-5), fresh implementer — review goes to a different agent | isolated worktree `.wt/quant-exl3-mul1`, based on `11fed3ba5`; CPU build local, CUDA arms built and run under an `rc` lease | `row/QUANT-EXL3-MUL1`, issue [#2495](https://github.com/mudler/vllm.cpp/issues/2495) | Owns ONLY: the codebook-2 arm of `Exl3DecodeCodeword` in `src/vt/cpu/cpu_exl3_dequant.cpp` and its declaration comment in `include/vt/ops.h`; the codebook range check in `vt::Exl3Gemm` (`src/vt/ops.cpp`); the `mul1` marker resolution in `include/vllm/model_executor/models/dense_weight_loaders.h`; `decode_mul1_product_2`, the `cb == 2` arm of `decode_3inst_2`, `dq_dispatch`'s widths, `Exl3ArmInstantiated`, `GemmKernelForShape` and the GEMV decline note in `src/vt/cuda/cuda_exl3.cu`; the new cases in `tests/vt/test_exl3_dequant.cpp`, `tests/vt/test_exl3_gemm.cpp` and `tests/vllm/model_executor/layers/test_exl3_native_loader.cpp`; `.agents/specs/quant-exl3-mul1.md`; the `QUANT-EXL3-MUL1` matrix row and the QUANT count in `scripts/check-agent-record.py`; and the EXL3 row of `docs/FEATURES.md`. EXCLUDES the GEMV kernel itself, which stays specialized to `bits == 3`; EXCLUDES the fused MoE arm, which stays `(3, 1)`; EXCLUDES every codebook-0 and codebook-1 decode path, which must stay byte-identical; and EXCLUDES any benchmark of `Mia-AiLab/Qwen3.8-27B-EXL3-3.5bpw`, which #2495 asks for and this row does not deliver | `ACTIVE` | 2026-09-01 — host decode RED-first (2 cases threw the by-name refusal, 87 assertions green) then GREEN, gated against hand-computed upstream literals rather than against this tree. Device arms need a lease; a CPU preflight cannot compile `cuda_exl3.cu` at all and is never reported as a device result |
