# CLAIM-QUANT-EXL3-PERF

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-QUANT-EXL3-PERF` | `QUANT-EXL3-PERF` (`ACTIVE`) | Claude Code (opus-5), fresh implementer — review goes to a different agent | slice A: isolated worktree `/home/mudler/_git/vllm.cpp-exl3perf`, based on `3d045ba1b`, leases staged at `/workspace/exl3perf-thor` and `/workspace/exl3perf-2570`. Slice B: isolated worktree `/home/mudler/_git/vllm.cpp-exl342`, based on `304787158`, leases staged at `/workspace/exl342-thor` (the numeric gate and the mutations) and `/workspace/exl342-dgx` (the throughput A/B). Every device build and run is under an `rc` lease; no box is reached by `ssh` | slice A `row/QUANT-EXL3-PERF`, slice B `row/QUANT-EXL3-PERF-42`; issue [#2570](https://github.com/mudler/vllm.cpp/issues/2570) for both | Owns ONLY: `Exl3GemvArmInstantiated` and `GemvKernel` in `src/vt/cuda/cuda_exl3.cu` together with the arm-set comment above them; **and, WIDENED for slice B, the `exl3_gemv_kernel` body and the `dq8_regs_*bits` extractors beside it** -- slice A's claim excluded the kernel body and said its `(4, 2)` widening stayed OWED, which is exactly the work slice B does, so the exclusion is lifted BY NAME rather than quietly overrun; the device case and the envelope cases in `tests/vt/test_exl3_gemv.cpp`; `.agents/specs/quant-exl3-perf.md`; the `QUANT-EXL3-PERF` matrix row and the QUANT count in `scripts/check-agent-record.py`; this row's entry in `RUNNABLE_BASELINE` in `scripts/check-gate-commands.py`; and the GEMV sentences of the EXL3 rows of `docs/FEATURES.md` and `docs/USAGE.md`, which said `(4, 2)` stayed owed. EXCLUDES the 2 bpw GEMV geometry and the `(4, 0)` / `(4, 1)` instantiations, which stay OWED; EXCLUDES `src/vt/exl3_policy.cpp`, whose envelope is upstream verbatim and unchanged; EXCLUDES the fused MoE arm, which stays `(3, 1)`; EXCLUDES the regular kernel's `Exl3ArmInstantiated` table, which `QUANT-EXL3-MUL1` owns; and EXCLUDES every codebook-0 and codebook-1 path, which must stay byte-identical | `ACTIVE` | 2026-09-03 — slice A landed and is device-gated on `thor:gpu0` (rel RMS 5.27980e-04 against tier 3c's 6.0e-3, 76/76, 0 skips); its dgx throughput A/B is still QUEUED. Slice B ports the kernel to 4 bpw and instantiates `(4, 2)`; spec committed before the code. Its numeric gate RAN on `thor:gpu0` and is GREEN: 184/184, 0 device skips, `(4,2)` narrow at rel RMS 5.09719e-04 and wide at 4.54155e-04 against tier 3c's 6.0e-3, both codebook-discriminated, and all three narrow arms REACHED UNFORCED byte-equal to their forced launch. Six mutations behaved exactly as pre-registered, M5 caught at compile time by `static_assert(LSTRIDE == TWORDS)` and M6 reddening only the three unforced assertions. Slice A's `dgx:gpu0` throughput A/B also ran: a NULL at 1.3% spread with `narrow_coresident` ceilinged at 144, so 34 of 409 modules are admitted. `(4,2)`'s own throughput leg did NOT run and is owed; `m == 8` is unmeasured at every width. A CPU preflight cannot compile `cuda_exl3.cu` at all and is never reported as a device result |
