# CLAIM-SAMPLE-PROMPT-LOGPROBS-W2

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SAMPLE-PROMPT-LOGPROBS-W2` | `SAMPLE-PROMPT-LOGPROBS` (`ACTIVE`) | Claude Code (claude-opus-5), fresh implementer | isolated worktree; CPU only, no GPU lease taken and none needed — every gate here runs on the CPU reference backend | `row/SAMPLE-PROMPT-LOGPROBS-W2`, issue [#1815](https://github.com/mudler/vllm.cpp/issues/1815) | Owns ONLY W2 of [`prompt-logprobs.md`](../specs/prompt-logprobs.md): the last hop that carries `RequestOutput.prompt_logprobs` onto the wire, and the three request validators upstream runs before the value is read. Touches `include/vllm/entrypoints/openai/protocol.h`, `include/vllm/entrypoints/openai/serving_utils.h`, `src/vllm/entrypoints/openai/{protocol,serving_utils,serving_completion,serving_chat}.cpp`, three existing test files and `docs/USAGE.md`. EXCLUDES the OpenAI `echo` serialization, which stays the row's named residual; excludes the `AsyncLLM` `n>1` fan-out ([#1816](https://github.com/mudler/vllm.cpp/issues/1816)) and the un-measured `ClampPromptLogprobs` call sites ([#1817](https://github.com/mudler/vllm.cpp/issues/1817)), both found here, both owed rather than fixed; excludes running any #1775 distribution diff, which needs the 27B checkpoint and a GPU | `ACTIVE` | 2026-08-23 — W2 implemented on the branch from base `bacb71109`. RED captured over a real socket against the reverted tree (`key 'prompt_logprobs' not found`; `stream=true` answered 200 and streamed). GREEN: `test_openai_protocol` 32/32 · 223, `test_openai_serving` 43/43 · 615, `test_openai_logprobs` 7/7 · 40, `test_openai_api_server` 76/76 · 1007, the five new cases carrying 173 assertions. Six reachability mutations, each printing `compile_rc` and `git diff --stat` and each restored byte-for-byte against a pre-taken sha256: five RED, M6 (the clamp call site) GREEN and owed. Not pushed, no pull request opened |
