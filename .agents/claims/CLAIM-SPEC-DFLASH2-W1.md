# CLAIM-SPEC-DFLASH2-W1

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DFLASH2-W1` | `SPEC-DFLASH2` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec `.agents/specs/dflash2-spec-decode.md` | isolated worktree, CPU only; no GPU, no oracle run, no lease, no checkpoint download | `row/SPEC-DFLASH2-W1`, issue [#1314](https://github.com/mudler/vllm.cpp/issues/1314) | Owns ONLY wave W1 of the spec's `## Work breakdown`: the `is_causal` precedence in `ResolveQwen3DFlashAttnModes` plus the key's carry in `MakeQwen3DFlashDraftConfig` (moved out of the loader's anonymous namespace so the carry is gateable); `SpeculativeConfig::IsDflash2Draft`; `ReadDflashDraftArchitectures` + `RefuseDflash2Draft` in the loader and their two call sites (the dflash branch of `LoadedEngine::ResolveSpecConfig`, and the early guard at the top of `LoadedEngine::FromModelDir`); the GGUF axis of both (`IsDflash2Gguf` plus the `dflash.attention.causal` read in `MakeDflashGgufConfig`, and the GGUF arm of `RefuseDflash2Draft`), the shared-test `BoolKv` helper; the two RED-first suites `tests/vllm/v1/spec_decode/test_dflash_causality.cpp` and `tests/vllm/entrypoints/test_dflash2_draft_routing.cpp`; the `SPEC-DFLASH2` row plus the two summary counts it moves; the spec's `## Now`; and the `docs/STATUS.md` and `docs/USAGE.md` entries. EXCLUDES every DFlash2 MECHANISM — the grouped dynamic convolution (W2), the candidate selector and its top-k (W3), the speculator and its device path walk (W4), the GGUF drafter ARM itself (W5) -- W1 refuses a DFlash2 GGUF, it does not load one, and the G1-G5 gates (W6). EXCLUDES any parity-pin advance, any DFlash1 behaviour beyond the shared causality rule, and the `DSparkDraftModel` classification owned by `SPEC-DSPARK-QWEN3-ROUTING` ([#1193](https://github.com/mudler/vllm.cpp/issues/1193)), whose engine-matrix anchors this change converts from LINE to SYMBOL form, because it inserted code above them and its first re-anchor applied the wrong shift | `ACTIVE` | 2026-08-19 — W1 landed red-first; both behaviours mutation-proven and the refusal's production call sites proven by deletion; full CPU ctest 510/510; extended after dispatch with the GGUF axis, gated synthetically and re-checked against both published GGUF artifacts |
