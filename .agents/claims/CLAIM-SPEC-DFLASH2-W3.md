# CLAIM-SPEC-DFLASH2-W3

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DFLASH2-W3` | `SPEC-DFLASH2` (`ACTIVE`), `KERNEL-DFLASH2-SELECTOR-EDGES` (`ACTIVE`), `KERNEL-TOPK-PAIRS` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec `.agents/specs/dflash2-spec-decode.md` | isolated worktree, CPU only; no GPU, no `nvcc`, no oracle run, no lease, no checkpoint download | `row/SPEC-DFLASH2-W3`, issue [#1314](https://github.com/mudler/vllm.cpp/issues/1314) | Owns ONLY wave W3 of the spec's `## Work breakdown`, plus the upstream-head reconciliation issue [#1404](https://github.com/mudler/vllm.cpp/issues/1404) names: the `vt::Dflash2SelectorEdges` op (`OpId::kDflash2SelectorEdges`, `Dflash2SelectorEdgesArgs`, the CPU reference and the CUDA mirror) and the `vt::TopKValuesIndices` op (`OpId::kTopKValuesIndices`, `TopKValuesIndicesArgs`, the CPU reference and the CUDA extension of the pivot-bracket threshold search), with their two kernel-matrix rows; the selector weights on `Dflash2SelectorWeights` / `Qwen3DFlashWeights` and their load in `LoadQwen3DFlash`, together with the `selector_rank`/`selector_top_k`/`output_multiplier`/`final_logit_softcapping` reads and the `input_embedding_scale` refusal; `Qwen3DFlash2Model::ComputeCandidates` / `::SelectorEdgeScores` and `RefuseQuantizedDflash2LmHead` in the new `qwen3_dflash2.{h,cpp}`; `vllm::v1::Dflash2SelectCandidates` and `RefuseDflash2PathWalk` in the new `spec_decode/dflash2/speculator.{h,cpp}`, replacing `RefuseDflash2CandidateSelector` and collapsing its two call sites into one shared function; the `lm_head_dequantized` carry through `LoadGgufSharedEmbedAndHeadBf16` and `SharedHeadSource`; the in-memory `DflashDraft` overload of `LoadedEngine`, which is what makes `GPUModelRunner::propose_drafts_block` reachable from a gate; the startup notice in `CheckDflash2DraftArm`; the suites `tests/vt/test_ops_dflash2_selector_edges.cpp`, `tests/vt/test_ops_topk_values_indices.cpp`, `tests/vllm/v1/spec_decode/test_dflash2_walk_refusal.cpp` (renamed from `test_dflash2_selector_refusal.cpp`) and `tests/vllm/v1/spec_decode/test_dflash2_runner_reach.cpp`, and the additions to `tests/vllm/models/test_qwen3_dflash2_draft.cpp` and `tests/vllm/entrypoints/test_dflash2_draft_routing.cpp`; the `SPEC-DFLASH2` engine-matrix row; the `KERNEL` count in `scripts/check-agent-record.py`; and the spec's `## Upstream chain`, `## Port map`, `## Risks/decisions` D11/D12, `## Owed` and `## Now`. EXCLUDES the device path walk, the realized-q draft-logit cache and the T>0 inverse-CDF arm (W4), the GGUF drafter ARM (W5), and the G1-G5 run gates (W6). EXCLUDES any parity-pin advance and any DFlash1 behaviour: every DFlash1 path is asserted unchanged | `ACTIVE` | 2026-08-20 — W3 landed with each added behaviour mutation-proven, its compile status and diff recorded, and a sha256-verified byte-for-byte restore; the selector is PRODUCTION-reached through `GPUModelRunner::propose_drafts_block` and that discharges spec `## Owed` O7; both CUDA arms written but never compiled (no `nvcc` on this host) and recorded as spec `## Owed` O10 |
