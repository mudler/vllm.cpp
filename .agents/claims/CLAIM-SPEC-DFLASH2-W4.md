# CLAIM-SPEC-DFLASH2-W4

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DFLASH2-W4` | `SPEC-DFLASH2` (`ACTIVE`), `KERNEL-DFLASH2-PATH-WALK` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec `.agents/specs/dflash2-spec-decode.md` | isolated worktree, CPU only; no GPU, no `nvcc`, no oracle run, no lease, no checkpoint download | `row/SPEC-DFLASH2-W4`, issue [#1314](https://github.com/mudler/vllm.cpp/issues/1314) | Owns ONLY wave W4 of the spec's `## Work breakdown`: the `vt::Dflash2PathWalk` op (`OpId::kDflash2PathWalk`, `Dflash2PathWalkArgs`, the CPU reference and the CUDA mirror) with its kernel-matrix row; `vllm::v1::Dflash2WalkPath` and `RefuseDflash1ArgmaxOnDflash2Block` in `spec_decode/dflash2/speculator.{h,cpp}`, replacing `RefuseDflash2PathWalk`; the DFlash2 draft arm of `GPUModelRunner::propose_drafts_block` and of `DflashProposeBlock`; the startup notice in `CheckDflash2DraftArm`; the removal of the checkpoint-derived `conv_block_size` seed in `LoadQwen3DFlash` (spec `## Owed` O5 item 1); the suite `tests/vt/test_ops_dflash2_path_walk.cpp`, the rename and rewrite of `tests/vllm/v1/spec_decode/test_dflash2_walk_refusal.cpp` into `test_dflash2_argmax_guard.cpp`, and the additions to `tests/vllm/v1/spec_decode/test_dflash2_runner_reach.cpp`, `tests/vllm/models/test_qwen3_dflash2_draft.cpp` and `tests/vllm/entrypoints/test_dflash2_draft_routing.cpp`; the `SPEC-DFLASH2` engine-matrix row; the `KERNEL` count in `scripts/check-agent-record.py`; `docs/STATUS.md`, `docs/BENCHMARKS.md` and `docs/SPECULATIVE-DECODING.md`; and the spec's `## Upstream chain`, `## Port map`, `## Risks/decisions` D13, `## Owed` and `## Now`. EXCLUDES the GGUF drafter ARM (W5) and the G1-G5 run gates (W6). EXCLUDES the PROBABILISTIC draft-sample arm and its realized-q cache, which no configuration can reach here and which `## Owed` O12 records. EXCLUDES any parity-pin advance and any DFlash1 behaviour: every DFlash1 path is asserted unchanged | `ACTIVE` | 2026-08-20 — W4 landed the device path walk with each added behaviour mutation-proven, its compile status and diff recorded, and a sha256-verified byte-for-byte restore; a DFlash2 draft now DRAFTS through the production runner and the reachability gate reads the drafts off the production trace and requires them to move with the selector's own scalars; the CUDA arm is written but has never compiled (no `nvcc` on this host) and is recorded as spec `## Owed` O11 |
