# CLAIM-SPEC-DFLASH2-W5

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DFLASH2-W5` | `SPEC-DFLASH2` (`ACTIVE`) | Claude Code (opus-5), helper role — fresh implementer working from the committed spec `.agents/specs/dflash2-spec-decode.md` | isolated worktree, CPU only; no GPU, no `nvcc`, no oracle run, no lease, no checkpoint download (the published GGUF arms were already resident on the NAS and only their headers were read) | `row/SPEC-DFLASH2-W5`, issue [#1314](https://github.com/mudler/vllm.cpp/issues/1314) | Owns ONLY wave W5 of the spec's `## Work breakdown`: the GGUF DFlash2 drafter arm. That is the DFlash2 metadata and the three optional output scalars in `MakeDflashGgufConfig`, the seven DFlash2 tensor names in `DflashGgufTensors::MapName`, the new `DflashGgufTokenizerVocab` and its use in `LoadQwen3DFlashFromGguf` (`src/vllm/model_executor/models/qwen3_dflash_gguf.{h,cpp}`); the removal of the GGUF startup refusal and the unified notice in `CheckDflash2DraftArm` (`src/vllm/entrypoints/model_loader.cpp`); the codebook-vs-target-vocabulary refusal in `Dflash2SelectCandidates` (`src/vllm/v1/worker/gpu/spec_decode/dflash2/speculator.cpp`); the suite `tests/vllm/models/test_qwen3_dflash2_gguf.cpp` with its own Q8_0 and Q4_K encoders, and the updates to `tests/vllm/entrypoints/test_dflash2_draft_routing.cpp` and `tests/vllm/v1/spec_decode/test_dflash2_runner_reach.cpp` that the moved notice makes stale; the `SPEC-DFLASH2` engine-matrix row; `docs/STATUS.md`, `docs/FEATURES.md` and `docs/SPECULATIVE-DECODING.md`; and the spec's `## Gates` G4, `## Risks/decisions` D10, `## Work breakdown`, `## Owed` and `## Now`. ADDS NO `vt` op, so it owes no kernel-matrix row and no `KERNEL` count change. EXCLUDES the G1-G5 run gates and `## Outcome` (W6). EXCLUDES the PROBABILISTIC draft-sample arm (`## Owed` O12). EXCLUDES keep-quant residency for a GGUF drafter, which is the DFlash GGUF lane's own design and is recorded as `## Owed` O13 with the cost measured. EXCLUDES any parity-pin advance and any DFlash1 behaviour: the DFlash1 GGUF path is asserted unchanged, silent as well as admitted | `ACTIVE` | 2026-08-20 — W5 landed the GGUF drafter arm. RED-first captured with its exit status; the name map gated BY CONSTRUCTION against a safetensors twin over 1 120 000 bf16 elements; the Q8_0 and Q4_K arms gated with a three-leg LOWER BOUND (block-encoded byte count, bit-exact values against the suite's own encoders, difference from the bf16 arm at tensor and block-logit level) because this lane dequantizes by design and a token gate cannot see a quantized tensor that never decoded; ten mutations each with its match count and compile rc printed, each restored sha256-verified and rebuilt; the published artifacts read header-only and the asset-gated case REPORTS rather than skipping in silence |
