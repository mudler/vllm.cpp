# CLAIM-SPEC-DRAFTER-CHAIN-W1

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DRAFTER-CHAIN-W1` | `SPEC-DRAFTER-CHAIN` (`ACTIVE`) | Claude Code (claude-opus-5), fresh implementer | isolated worktree; CPU only, no GPU lease taken and none needed — G1 and G5 are both device-free | `row/SPEC-DRAFTER-CHAIN-W1`, issue [#1522](https://github.com/mudler/vllm.cpp/issues/1522) | Owns ONLY W1 of [`drafter-chain.md`](../specs/drafter-chain.md): the `vllm_cpp.drafter_chain` field of `--speculative-config` — its parsing, its validation, its refusals, and the loader refusal that keeps the stored field reached. Touches `include/vllm/config/speculative.h`, `src/vllm/config/speculative.cpp`, the chain guard in `LoadedEngine::ResolveSpecConfig` and `LoadedEngine::FromModelDir` in `src/vllm/entrypoints/model_loader.cpp`, two new test files and their CMake registrations. EXCLUDES per-drafter attribution and counters (W2), chain resolution (W3), acceptance accounting (W4), measurement (W5), and every change to which speculator drafts. W3 in particular must not start before `SPEC-DFLASH2` W6 produces a measured number, and this claim does not touch that row's files | `ACTIVE` | 2026-08-21 — W1 landed on the branch, then REPAIRED against a fresh review and re-merged onto `origin/main`. G1 and G5 PASS, re-measured on the merged tree: `test_speculative_drafter_chain` 13 cases / 149 assertions / 0 failed, `test_drafter_chain_reach` 7 cases / 78 assertions / 0 failed, both `Status: SUCCESS!`. The 136 recorded here before the repair never reproduced — the value on that head was 135 — so both numbers above are measured rather than carried forward. Eleven mutations at first landing plus three in the repair, all detected: the two production call sites, the parsed chain order, `use_drafter_chain()` narrowed to `size() > 1` (caught only after the repair added a ONE-entry loader case), and the class-2 entry-key branch ([#1598](https://github.com/mudler/vllm.cpp/issues/1598)). G2, G3 and G4 remain PENDING with no instrument; W2 through W5 unclaimed |
