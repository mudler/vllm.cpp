# CLAIM-SPEC-DSPARK-BLOCK-SIZE-GUARD

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DSPARK-BLOCK-SIZE-GUARD` | `SPEC-DSPARK-BLOCK-SIZE-GUARD` (`ACTIVE`) | Claude Code (opus-5), fresh implementer | isolated worktree, CPU-only build, no GPU lease and no publication | `row/DSPARK-BLOCK-SIZE-GUARD`, issue [#1225](https://github.com/mudler/vllm.cpp/issues/1225) | Owns ONLY the DSpark speculative-config RESOLUTION of [`dspark-block-size-guard.md`](../specs/dspark-block-size-guard.md): the new `ReadDsparkDraftKeys` helper in `src/vllm/entrypoints/model_loader.cpp`, the `n_predict` / block-floor / block-key arguments at the single remaining `ResolveDspark` call, inside `LoadedEngine::ResolveSpecConfig`, the deletion of the second resolution in `LoadedEngine::FromModelDir` and the hoisted delegation that replaces it, the narrowed k-required throw in the same branch, the `block_size_key` parameter of `SpeculativeConfig::ResolveDspark` in `include/vllm/config/speculative.h`, the `ResolveSpecConfig` access change in `include/vllm/entrypoints/model_loader.h`, and `tests/vllm/entrypoints/test_dspark_block_size_guard.cpp`. EXCLUDES `SpeculativeConfig::IsDsparkDraft`, `ResolveDsparkArchitecture` and the method/architecture classification in `ResolveSpecConfig`, all of which belong to `SPEC-DSPARK-QWEN3-ROUTING` ([#1193](https://github.com/mudler/vllm.cpp/issues/1193)) on a parallel branch; also excludes the DSpark speculator, the weight path, and the DeepSeek-V4 DSpark runtime | `ACTIVE` | 2026-08-18 — W1-W3 landed and both fresh-review findings repaired. Red captured before each change; focused green 14/14 with 39 assertions; the reachability mutations red it 8/14 (the `ResolveSpecConfig` arguments) and 2/14 (the deleted second resolution), each with the file restored byte-for-byte and its sha256 rechecked. A second review returned `PASS` on the code; its three record findings are repaired in the spec's §6c, including one guarantee the speculators cases were credited with and do not carry. G3 (run) and G4 (spec-off) owed, both needing a GPU lease |
