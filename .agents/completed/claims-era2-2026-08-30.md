# Retired claims - era 2 (2026-08-30)

Claims whose every row left `SPIKE`/`ACTIVE`, kept verbatim as the record of
who held what — the era-1 precedent (`claims-era1-2026-08-04.md`) continued.
Nothing here is load-bearing for live coordination: the row records live in
the matrices, and a retired claim never blocks a new one.

## `CLAIM-BACKEND-TENSTORRENT-GDN`

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-BACKEND-TENSTORRENT-GDN` | `BACKEND-TENSTORRENT-GDN` (`ACTIVE`, new) | Maki (zai-glm-5.3), helper role per `scripts/agent-role.py`, working from the committed spec `.agents/specs/tenstorrent-gdn.md` | isolated worktree `/home/lu_zero/Sources/vllmcpp-tt-gdn`; real Blackhole P150 (`thalia`, local board, `flock` mutex per GPU rule — not an `rc` fleet device) | `row/BACKEND-TENSTORRENT-GDN`, base `origin/main` `1db7e59cf`, issue [#1715](https://github.com/mudler/vllm.cpp/issues/1715) | Owns ONLY the GDN op chain as native TT kernels + `RegisterOp` wiring + op-level gates vs the CPU f32 oracle: W1 prefill set (`kL2Norm`, `kRmsNormGated`, `kCausalConv1dFwd`, `kGdnPrefill`) and W2 decode set (`kCausalConv1dUpdate`, `kGdnDecode` both `state_idx` forms, `kGdnStateGather`, `kGdnStateScatter`), the spec, the backend-matrix row, and the issue-index row. EXCLUDES architecture allow-list registration for `Qwen3_5*` and the capacity decision (the wiring row, owed in the spec's `## Owed`; the ops land production-unreached until it exists, disclosed in the landing commit body and PR body per Nothing-lands-dead); `kGdnSpecDecode`/`kGdnPackedDecode` (owe #1627 first); MoE, quant arms, the VL half; anything under `src/vt/cuda/`, `src/vt/cpu/` (the oracle stays untouched), or `src/vllm/model_executor/models/qwen3_5.cpp` (no model-side edits; `IndexedGdnOpsNative` starts answering TRUE the day the arch row lands, which is intended) | `ACTIVE` | 2026-08-22 — issue #1715 filed; spec committed spec-first with the two record rows; implementation waves W1/W2 owed |

Retired when `BACKEND-TENSTORRENT-GDN` reached `DONE` (matrix owner `337f6e07a`,
the row's W2 decode+state-I/O commit; the lifecycle move is the record commit on
`row/BACKEND-TENSTORRENT-GDN`, closing #1715). The claim held W1 and W2 through
fresh-review PASS and the production reach through the `Qwen3_5*` wiring row
(e2e sacred pair 16/16 STRICT on both legs).
