# Retired claims - era 4 (2026-09-21)

Claims whose every row left `SPIKE`/`ACTIVE`, kept verbatim as the record of
who held what — the era-1 (`claims-era1-2026-08-04.md`), era-2
(`claims-era2-2026-08-30.md`), and era-3 (`claims-era3-2026-09-05.md`)
precedents continued. Nothing here is load-bearing for live coordination: the
row records live in the matrices, and a retired claim never blocks a new one.

## `CLAIM-MODEL-LAYA`

# CLAIM-MODEL-LAYA

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-MODEL-LAYA` | `MODEL-LAYA` (`DONE`) | Claude Code (glm5.2), operator role | `.wt/laya` | `row/MODEL-LAYA` | Laya port: ModernBERT-large encoder with sliding-window + dual-RoPE, transformer decision head, scorer, act_head, sequence construction, model registration, SystemOne API serving, CPU + GPU | `DONE` | 2026-09-21 |

Retired when `MODEL-LAYA` reached `DONE` (matrix owner `c0320715f`, the
closing commit; the code landed on `main` through PR #3263). E2E tested through
LocalAI `/v1/systemone` (choice/score/noul all pass).

## `CLAIM-MODEL-KEV`

# CLAIM-MODEL-KEV

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-MODEL-KEV` | `MODEL-KEV` (`DONE`) | Claude Code (glm5.2), operator role | `.wt/kev` | `row/MODEL-KEV` | kev port: Qwen3.5-0.8B-Base backbone ForwardHidden, convert-time LoRA merge (r=16), PointerHead readout, sequence construction, model registration, SystemOne API serving, CPU + GPU | `DONE` | 2026-09-21 |

Retired when `MODEL-KEV` reached `DONE` (matrix owner `4756d264c`, the closing
commit; the code landed on `main` through PR #3295). E2E tested through LocalAI
`/v1/systemone` (choice/score/noul all pass).

## `CLAIM-MODEL-CUA-S1-FORMS`

# CLAIM-MODEL-CUA-S1-FORMS

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-MODEL-CUA-S1-FORMS` | `MODEL-CUA-S1-FORMS` (`DONE`) | Claude Code (glm5.2), operator role | `.wt/cua-s1` | `row/MODEL-CUA-S1-FORMS` | cua-s1-forms port: TinyTransformerScorer (byte-level embedding, 2-layer context encoder, 1-layer option encoder, attention head), ByteCollator tokenization, weight loading, `/v1/score` endpoint serving, CPU | `DONE` | 2026-09-21 |

Retired when `MODEL-CUA-S1-FORMS` reached `DONE` (matrix owner `53f018247`,
the closing commit; the code landed on `main` through PR #3265). E2E tested
through LocalAI `/api/score` (2 and 3 candidate cases pass).
