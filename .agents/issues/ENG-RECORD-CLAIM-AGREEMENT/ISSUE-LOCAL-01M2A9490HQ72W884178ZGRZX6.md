ID: ISSUE-LOCAL-01M2A9490HQ72W884178ZGRZX6
Title: Four records the tree falsifies, found by the sweep that followed `97cb6964b`, each stale in a way no checker can see. (1) `.agents/upstream-sync.md` and `.agents/NOW.md` call the declared token-exact gate at pin `e126687a9a` owed by #2794, which closed COMPLETED on 2026-09-06; NOW.md contradicted itself, recording that gate as run-and-PASSED two sentences earlier. What #2794 discharged is ONLY the OPT-125m gate at six prompts and sixteen tokens. TWO obligations survive at this pin and both are restated wherever the closure is: the BENCHMARK step-6 re-baseline (#2818, OPEN), and the other four strict goldens -- 27B W4A4, 32B-NVFP4A16, 35B, Coder -- which carry NO issue by design and live under `## Owed` in `.agents/specs/upstream-sync-headpin-tokengate.md:569`, as `.agents/oracles/vllm.md:93` also states. THREE further live sites anchored owed work on the closed #2794 and are reconciled the same way rather than deleted: `.agents/specs/pin-advance.md` (whose OPT half #2794 really did answer), `.agents/specs/glm4-moe-lite-router-f32.md` (whose `Glm4MoeLiteForCausalLM` re-capture #2794 never covered and which, stated in those words, has no tracker at all), and `.agents/specs/upstream-pin-advance-e126687.md` -- the wave spec that DECLARED the gate -- whose §5.1 heading, §5.1 body, §7 risk row and `## Owed` bullet all still read the gate as unrun, under no supersession banner. An earlier draft of this title said TWO and missed that third file, which is this row's own defect shape: a fact corrected in one place and left standing in another. Its §3 table at `:104` is NOT reconciled and is not a miss: that table is explicitly scoped to "their state at this change's merge base `23ac6f1a7`" and is defensibly historical. A FOURTH site is reconciled and is a different shape, so it is not counted among those three: `.agents/oracles/vllm.md`'s `gateable` residual paragraph named the #2794 token gate on `dgx:gpu0` as "the obvious candidate" for the first row at `e126687a9a`, which the device table three lines ABOVE it in the same file already falsifies -- that row is at `e126687a9a`, captured 2026-09-04. That is a stale forward-looking sentence rather than an owed-work anchor, and the same file at `:93` already states the split correctly, so the repair narrows the residual to the one board and one gate model it really covers instead of retiring it. The `.agents/sync/` wave reports that still name #2794 as owed are NOT reconciled and are not misses either: a sync report records what was true when it was written, which `.agents/specs/upstream-pin-advance-e126687.md` states in its own stop conditions. (2) `.agents/specs/glm5-next-flash.md` heads a section "transformers -- the ONLY admissible reference", which vLLM falsified on 2026-09-03 by registering `Glm5NextForConditionalGeneration` (`98ed0856f3`, registry.py:429 on main); `.agents/oracles/transformers.md` has recorded that lane EXPIRED since, and the file's own head-level banner at `:3-6` already records the supersession and points at `.agents/specs/glm5-next-upstream-reconciliation.md` under row `MODEL-MM-GLM53-FLASH` (#3045). So this is NOT a tree that disagreed with itself at the top level: the verdict was already recorded and correct, and what stood stale was this SECTION, which kept the pre-registration reading in its own heading underneath a banner that had superseded it. The repair carries the banner down to the section rather than issuing a new verdict. (3) `.agents/model-matrix.md` stored "366 architecture rows" against 377 today. (4) `.agents/kernel-matrix.md`'s `## Count invariants` stored "exactly 38 practical kernel-family rows" plus a seven-number lifecycle baseline summing to the same stale 38, against 60 `KERNEL-*` rows today. Both counts are the shape AGENTS.md Records forbids -- a stored measurement of one file inside that same file -- so each is replaced by a DATED DERIVATION that says in its own words that it is evidence of the drift and not an invariant, names `scripts/check-agent-record.py` as where the live cardinality is read (MODEL=381, KERNEL=60 on 2026-09-12), and expects to go stale. No checker enforces any of these numbers, and none is restated as a count that a later edit must maintain
Row: ENG-RECORD-CLAIM-AGREEMENT
State: OPEN
Kind: record
GitHub: -
Mirror: PENDING
Availability: FULL
Created: 2026-09-12
Updated: 2026-09-12
Closed: -

## Problem

Four stale records, two of them stored counts that drifted twice.

## Resolution

-
