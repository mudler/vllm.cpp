# Upstream sync report, 2026-09-03, target `e126687a9a`, STEP 7 — THE PIN MOVED

Wave `PINADVANCE`. This report advances the parity pin. It ports nothing and it
measures nothing on a device.

Issue: [#2817](https://github.com/mudler/vllm.cpp/issues/2817).
Spec: [`../specs/upstream-pin-advance-e126687.md`](../specs/upstream-pin-advance-e126687.md).
Predecessors: [`2026-09-02-e126687.md`](2026-09-02-e126687.md) (install half),
[`2026-09-03-e126687-runhalf.md`](2026-09-03-e126687-runhalf.md) (run half),
[`2026-09-03-e126687-step6.md`](2026-09-03-e126687-step6.md) (STEP6, which found
the ordering defect), and `2026-09-03-portq{1..7}.md`.

| Field | Value |
|---|---|
| Prior pin | `5559679229bc961848b121ccdeaa8fa5d79bec98`, 2026-07-26 |
| **New pin** | **`e126687a9a828d513c01a07cd69f025f27d63280`, 2026-08-31, vllm#53896** |
| Commits in range | 1465 |
| Device used | **none.** No build, no lease, no GPU, no golden re-capture |
| Pin moved | **YES** |

## 1. The ruling, and it is the authority for this wave

`.agents/upstream-sync.md` §"The sync cycle" orders step 6 before step 7. STEP6
([#2771](https://github.com/mudler/vllm.cpp/issues/2771), landed as
[#2783](https://github.com/mudler/vllm.cpp/pull/2783)) established by hermetic
probe over the committed `record_oracle_manifest` that this order **cannot be
executed here**. `tools/bench/online_gate.py` refuses on
`VLLM_DISTRIBUTION_VERSION` and `VLLM_ORACLE_VERSION` at `:3529-3540`, on the
commit at `:3542`, and on FlashInfer at `:3552-3560`. All four come from the
` ```parity-pin ` block by way of `tools/bench/serve_low_common.py:100-109`, and
editing that block **is** step 7. STEP6 filed the ordering `NEEDS_DECISION` and
refused to resolve it by loosening an assertion.

**The developer ruled: advance the pin first, then re-run step 6 against it.**

This report records that as a **developer decision that inverts a documented step
order**, not as a reading the protocol permits, and carries its consequence with
it:

> **Step 6 becomes post-hoc validation. A red at that point requires REVERTING
> the pin, not holding it while the affected rows are re-argued.**

Owed by [#2818](https://github.com/mudler/vllm.cpp/issues/2818). The same
sentence is in that issue, in `.agents/upstream-sync.md`, in the spec §1, in
`docs/benchmarks/how-we-measure.md` and in this change's pull request body,
because a ruling recorded in one place is a ruling one reader can miss.

**No checker enforces the inversion and none was added.** A gate over step order
would have to read the same block it is deciding about, which is the circularity
this wave exists to get out of.

## 2. The block, field by field

Every value was MEASURED at the target by a predecessor wave and is transferred
here, not transcribed from a release number.

| Field | Before | After | Measured where |
|---|---|---|---|
| `vllm_commit` | `5559679229bc961848b121ccdeaa8fa5d79bec98` | `e126687a9a828d513c01a07cd69f025f27d63280` | the target |
| `vllm_runtime_version` | `0.23.1rc1.dev1511+g555967922` | `0.28.1rc1.dev132+ge126687a9` | `IMPORT VLLM_VERSION`, read from `cd /`: [`2026-09-02-e126687.md`](2026-09-02-e126687.md) §5.4, reproduced by the SOURCE build in [`2026-09-03-e126687-runhalf.md`](2026-09-03-e126687-runhalf.md) §2 (`VLLM_VERSION 0.28.1rc1.dev132+ge126687a9`) |
| `vllm_distribution_version` | `0.23.1rc1.dev1511+g555967922.precompiled` | `0.28.1rc1.dev132+ge126687a9.precompiled` | `DIST DIST_VERSION`, [`2026-09-02-e126687.md`](2026-09-02-e126687.md) §5.4 |
| `flashinfer_version` | `0.6.15.post1` | `0.6.18` | `PIPLIST flashinfer-python 0.6.18`, [`2026-09-02-e126687.md`](2026-09-02-e126687.md) §5.2 |

The `+g<sha>` constraint holds. `+ge126687a9` is a 9-character prefix of the
target commit, above the 7-character floor `tests/tools/test_oracle_pin.py` pins,
so `assert_oracle_commit` accepts it.

### 2.1 The `.precompiled` suffix is a build-mode property, and this is the risk

The distribution string was measured under `VLLM_USE_PRECOMPILED=1`. **The run
half's SOURCE build at the same revision produced a wheel with NO suffix**:
`vllm-0.28.1rc1.dev132+ge126687a9-cp312-cp312-linux_aarch64.whl`
([`2026-09-03-e126687-runhalf.md`](2026-09-03-e126687-runhalf.md) §2). Since
`online_gate.py` compares this field for equality, a source-built oracle is
refused on it.

The measured value is recorded anyway, because the alternative is writing a
string nobody read, which is #520 exactly. Whoever runs #2818 records which build
mode their oracle used and corrects the field from THAT measurement if it is
wrong — never by editing the block to make a run pass. The same relationship held
at the prior pin, whose block also carried `.precompiled`.

### 2.2 One prior-pin discrepancy does NOT carry forward, and the reason is measured

[#1185](https://github.com/mudler/vllm.cpp/issues/1185) recorded a lease build of
the prior pin reporting `0.1.dev1+g555967922` against the block's
`0.23.1rc1.dev1511+g555967922`, caused by a shallow fetch leaving
`setuptools_scm` unable to count commits since the last tag. The target's own
build recorded `SHALLOW=false REVCOUNT=20591
GIT_DESCRIBE=v0.28.1rc0-132-ge126687a9`
([`2026-09-03-e126687-runhalf.md`](2026-09-03-e126687-runhalf.md) §2), which is
why its full string is the one the block can carry. The mechanism is general and
#1185 stays open.

## 3. What the advance rests on, including one thing that is not on `main`

`.agents/oracles/vllm.md` named four owed items before this wave.

| Owed item | State at merge base `23ac6f1a7` |
|---|---|
| 1. The 290-entry PORT-NOW queue | Reconciled and ruled **not a blocker** — in [#2768](https://github.com/mudler/vllm.cpp/pull/2768), which is **OPEN and NOT MERGED** |
| 2. A declared token-exact gate at the target | **OWED, unmeasured** ([#2794](https://github.com/mudler/vllm.cpp/issues/2794)) |
| 3. Step-6 re-measurement | **OWED, now post-hoc** ([#2818](https://github.com/mudler/vllm.cpp/issues/2818)) |
| 4. A reading on `dgx:gpu0` | **OWED**, only `thor:gpu0` was measured ([#2611](https://github.com/mudler/vllm.cpp/issues/2611)) |

**Item 1's discharge is in an unmerged pull request, and this wave states that
rather than inheriting the claim.** #2768 reports `state: OPEN`,
`mergedAt: null`, `headRefName: row/PORTQ-RECONCILE-W1`, and `.agents/sync/` on
`origin/main` carries no reconciliation report; the only reference to it in the
tree is `2026-09-03-e126687-step6.md:10`, which cites it as a pull request. The
seven PORTQ tranche reports ARE on `main`, so the classification is landed; what
is not landed is the pass that ruled the queue discharged. **The advance proceeds
on the developer ruling, and this is the one input it takes on trust.**

Items 2, 3 and 4 stand. §5 says what that means.

## 4. The records the advance falsifies, and how the count was reached

Each check read its return code into its own variable rather than after a pipe,
and each zero is paired with a positive control through the same probe form,
because on this row a zero has meant eight different things.

### 4.1 `.agents/porting.md` — ModelOpt fp8's `out_dtype`

The too-wide-dtype table taught `torch.get_default_dtype()`. vllm#48861
(`0b37d8389f`) replaced it with `get_current_vllm_config().model_config.dtype` in
all three `ModelOptFp8*LinearMethod.__init__`.

```console
$ git grep -n 'get_default_dtype' e126687a9a -- vllm/model_executor/layers/quantization/modelopt.py
TARGET_GETDEF_RC=1                                  # the finding
$ git grep -n 'self.out_dtype' e126687a9a -- vllm/model_executor/layers/quantization/modelopt.py
TARGET_OUTDTYPE_RC=0                                # the control: 6 hits, same file, same revision
```

Repaired, with the commit named, because the line is the guide a reader consults
for exactly the check that commit is about.

### 4.2 The `calculate_kv_scales` anchors — SEVEN sites, and the count has been wrong three times

vllm#49389 (`dd11df04f3`) DELETES the field.

```console
$ git grep -n 'calculate_kv_scales' 5559679229 -- vllm/config/cache.py
5 hits, the field at :111
$ git grep -n 'calculate_kv_scales' e126687a9a -- vllm/config/cache.py
RC=1                                                # the finding
$ git grep -n 'cache_dtype' e126687a9a -- vllm/config/cache.py
RC=0, 5 hits                                        # the control
```

Tree-wide at the target the identifier survives only in one unrelated comment
(`vllm/models/kimi_k3/nvidia/mla.py:473`), so the target is gone rather than
moved.

**The count went two, then five, then six, always low, always the same
mechanism**: a `cache.py:111` grep cannot see a citation that WRAPS across a
line. This wave re-derived it from the SYMBOL instead and finds **seven**.

| # | Site | Form | A `cache.py:111` grep finds it? |
|---|---|---|---|
| 1 | `include/vllm/config/cache.h:2` | `calculate_kv_scales:111` | **No** — the filename is on line 1 |
| 2 | `include/vllm/model_executor/layers/quantization/kv_cache.h:90` | `` `cache.py:111` `` | Yes |
| 3 | `include/vllm/model_executor/layers/quantization/kv_cache.h:108` | **inside the thrown `VT_CHECK` message a USER reads** | Yes |
| 4 | `tests/vllm/entrypoints/test_kv_cache_fp8_wiring.cpp:8` | `` `:111` calculate_kv_scales `` | **No** — wraps from `:7` |
| 5 | `tests/vllm/entrypoints/test_kv_cache_fp8_wiring.cpp:680` | `(cache.py:111)` | Yes |
| 6 | `.agents/specs/fp8-kv-cache.md:1044` | `` (`config/cache.py:111`) `` | Yes |
| 7 | `.agents/specs/fp8-kv-cache.md:46` | `` `:99-104` `` | **No** — a DIFFERENT anchor for the same deleted field |

Site 7 is the one every prior count missed. `2026-09-03-portq2.md:321` ruled it
"adjacent rather than a seventh instance", which is right about the `:111` STRING
and wrong about the obligation: the field is deleted, so an anchor pointing at it
is falsified whatever number it carries. **Site 7 was also wrong at the PRIOR
pin** — `vllm/config/cache.py:99-104` at `5559679229` is
`prefix_caching_hash_algo`'s docstring. It is repaired here rather than left as a
second defect nobody owns. An eighth mention at `fp8-kv-cache.md:32` carries no
line anchor and called the flag "deprecated"; it now says deleted.

**Behaviour does not change.** `ResolveKvCacheScales` still takes the parameter
and refuses it by name. The thrown message now says the option was REMOVED
upstream and names vllm#49389, so a user who reads it can find out what happened
to their flag.

**No historical record was edited.** `2026-09-01-cdefd9d.md`,
`2026-09-03-portq2.md`, `../specs/upstream-sync-portq2.md` and
`.agents/completed/**` describe what a past wave read at the pin it read it at.

### 4.3 `include/vllm/config/speculative.h` — a comment whose instruction came due

The comment above `async_scheduling_compatible()` said `"draft_model"` is refused
AT THE PIN and told the reader to reconcile when the pin advances past
`b389ac2946`. `c0202c5603` (vllm#48341) is the commit that does it:

```console
$ git merge-base --is-ancestor c0202c5603 e126687a9a828d513c01a07cd69f025f27d63280
ANCESTOR_OF_TARGET_RC=0
$ git merge-base --is-ancestor c0202c5603 5559679229bc961848b121ccdeaa8fa5d79bec98
ANCESTOR_OF_PIN_RC=1
```

The comment now says the pin HAS advanced past it, that upstream auto-enables
async scheduling for a `draft_model` speculator and this tree does not, and names
[#2649](https://github.com/mudler/vllm.cpp/issues/2649). **The predicate is NOT
changed here**: #2649 is product work with a behaviour test and its own fresh
review, and `../upstream-sync.md` §Rules forbids mixing feature work into a sync
cycle. **#2649 is unblocked by this merge**, and until it lands the tree
knowingly disagrees with its own pin at that line.

### 4.4 Measured, not guessed: what else the advance falsified

`scripts/check-symbol-anchors.py --upstream-root <pinned vllm>` was run BEFORE and
AFTER the block edit, on the same tree, against the same checkout. It is not a CI
gate and it is red at both pins, so the result is the DELTA:

```console
before (5559679229): upstream checked 389 (fresh 372, stale 6, file absent 11)  -> 15 stale
after  (e126687a9a): upstream checked 389 (fresh 359, stale 4, file absent 26)  -> 28 stale
```

Fifteen citations went stale, two were REPAIRED by the advance
(`model-matrix.md::GlmMoeDsaForCausalLM`, which the target registers, and
`dflash2-spec-as-decode.md::_make_xqa_draft_block_mask`), and one apparent move
(`fp8-kv-cache.md:128` to `:133`) is the same citation relocated by this wave's
own edit.

### 4.4a The thirteen matrix rows are THREE different things, and two of them are models we SHIP

**An earlier draft of this report called all thirteen "rows for architectures
vllm#53608 deleted". That is wrong, and it matters more than a miscount**,
because it files two live mirror obligations as matrix cleanup. A fresh review of
#2827 caught it. Attributed per file, `git log --diff-filter=D` over the range:

| Deleting commit | What it is | Files |
|---|---|---|
| `7ca336929c` vllm#53608, "Remove ten deprecated model architectures" | genuine deprecation | **8**: `arctic`, `fairseq2_llama`, `gritlm`, `hyperclovax_vision`, `mpt`, `chameleon`, `cheers`, `fireredlid` |
| `48d7132962`, "Migrate FlexOlmo, Olmo3 and Hunyuan V1/VL to the Transformers modeling ..." | **MIGRATION to the Transformers fallback**, not removal | **4**: `flex_olmo`, `hunyuan_v1`, `hunyuan_vision`, `olmo3` |
| `aeeb36b1f1` vllm#50000, "[New model] Kimi K3" | **RELOCATION**, still registered | **1**: `kimi_linear` |

So only 8 of 13 are deprecation. The other five are upstream restructuring, and
two of them reach models this project ships:

- **`KimiLinearForCausalLM` was relocated, not removed.** At the target
  `registry.py:144-147` maps it to `("vllm.models.kimi_k3",
  "KimiLinearForCausalLM")`, where at the pin `registry.py:140` mapped it to
  `("kimi_linear", "KimiLinearForCausalLM")`. **We ship it**:
  `src/vllm/model_executor/models/kimi_linear_registry.cpp:166` registers it,
  `docs/FEATURES.md:159` publishes it with a speed row, and
  `.agents/model-matrix.md:121` is a `🚧` row, not `INVENTORIED`. Its mirror
  source moved under a shipped model.
- **`Olmo3ForCausalLM` now routes to the generic Transformers fallback.**
  `registry.py:691` at the target reads `("transformers",
  "TransformersForCausalLM")`; `registry.py:181` at the pin read `("olmo3",
  "Olmo3ForCausalLM")`. **We ship it** as an Olmo2 factory alias
  (`src/vllm/model_executor/models/olmo2_registry.cpp:127`,
  `.agents/model-matrix.md:136` is `✅`). That is a live mirror divergence.
  **The anchor checker cannot see it** — our row cites `olmo2.py`, which still
  exists — so it is not among the fifteen and would have gone unnoticed by the
  instrument that found the rest.

`7ca336929c` was classified **IGNORE** by two waves on the ground that we
register none of them, which is right about the PORT for those 8 and does not
cover the matrix rows that cite the files, nor the two shipped models above.

**The other two of the fifteen are not model rows at all**, and are named here
rather than left to the reader:

- `.agents/specs/moe-semantics.md:29` cites
  `vllm/model_executor/layers/fused_moe/cpu_fused_moe.py::select_experts`. That
  FILE is gone at the target (`RC=128`; it exists at the pin, `RC=0`), and
  `select_experts` now lives in
  `fused_moe/experts/cpu_moe.py` and `fused_moe/router/fused_moe_router.py`. A
  move, not a deletion.
- `.agents/specs/gdn-state-kv-budget.md:101` cites
  `vllm/v1/core/kv_cache_utils.py::get_num_blocks`. **The free function is gone
  from that file** — `5559679229:vllm/v1/core/kv_cache_utils.py:993` defines it,
  the target's copy of the same file has no match (`RC=1`) while the file itself
  is present (control `RC=0`). Three `def get_num_blocks` exist elsewhere at the
  target (`kv_cache_coordinator.py`, `single_type_kv_cache_manager.py`,
  `kv_offload/tiering/example/manager.py`), and they are methods, not the
  free function the spec cites. Whoever re-anchors it must decide which, if any,
  is the successor.

All of it is [#2819](https://github.com/mudler/vllm.cpp/issues/2819) and **owed,
not fixed here**: it is a per-row job across a keyed matrix with many writers,
and the two shipped-model items are mirror work that needs their own rows.

**This checker is the reason §4.1 and §4.2 are not the whole answer.** Both were
handed to this wave. §4.4 is what an instrument found that nobody had named, and
its two REPAIRS are what stop the reading being "the advance only breaks things".

## 5. What the advance costs, said before anything else can imply otherwise

### 5.1 No gate has run at this pin

The 2026-07-26 advance re-captured goldens on the new oracle on GB10 and recorded
zero real drift (`../specs/pin-advance.md` §7 W3b). **This advance has no such
pass.** Every committed golden was captured against `555967922` or earlier. The
declared token-exact gate at the target is
[#2794](https://github.com/mudler/vllm.cpp/issues/2794), which also records that
the OPT golden was never re-validated at the PRIOR pin either. The run half's
six-prompt, sixteen-token agreement with `opt_greedy` is informative and is not a
gate; the wave that measured it said so
([`2026-09-03-e126687-runhalf.md`](2026-09-03-e126687-runhalf.md) §7 C2).

### 5.2 Gateability: exactly what "runs a model" claims

`gateable = yes` is set on the oracle record with `evidence` pointing at the run
half. **Established**: at `e126687a9a`, on `thor:gpu0` (aarch64, `sm_110`, driver
595.78), vLLM builds from source in 94 minutes, ships seven compiled extensions
with a kernel EXECUTED on device, imports as `0.28.1rc1.dev132+ge126687a9` read
from `cd /`, and serves greedy tokens on FLASH_ATTN/FA2 in eager AND compiled
configurations.

**Not established**, and the pin's movement makes none of it true: `qwen4_exp`
does not run on this fleet at this revision
([#2626](https://github.com/mudler/vllm.cpp/issues/2626), QSA `cooperative_topk`
cluster misconfiguration, cause unestablished, and the safetensors arms exceed
the largest fleet box); no reading exists on `dgx:gpu0`, which is the `sm_121a`
device every binding number in this tree was taken on; `facebook/opt-125m` is not
a gate model; nothing about any other board.

**"The oracle builds and runs" and "this row can be gated against it" are
different statements.** The oracle record keeps them apart in prose beside the
block, and its device-scoped table keeps its rule: absence means unmeasured,
never unsupported.

### 5.3 Every ported file header now names a revision BELOW the pin

Step 5 did not run, so no header was bumped. **The answer is a FLOOR: at least
177 files.** It is labelled one because no probe here can be shown to be
complete.

**This number was wrong twice, and the second time this section's own §4.2
explains exactly why.** The first figure was "roughly 500" — a guess, never
measured. The second was 90, from `grep -rlE 'Ported from.*55596792'`, and that
is a **line-scoped** predicate applied to a header that WRAPS. It is the same
mechanism §4.2 documents for the `calculate_kv_scales` anchors, in the same
change, one section apart. A fresh review of #2827 caught it. The controlled
example, rc read directly:

```console
$ sed -n '1,2p' include/vllm/v1/spec_decode/draft_model_proposer.h
// Ported from:
//   * vllm/v1/spec_decode/draft_model.py:19 (DraftModelProposer) @ 555967922
$ grep -cE 'Ported from.*55596792' include/vllm/v1/spec_decode/draft_model_proposer.h
0     RC=1        <- the finding
$ grep -cE 'Ported from' include/vllm/v1/spec_decode/draft_model_proposer.h
1     RC=0        <- the positive control, same file, same probe form
```

`include/vllm/v1/attention/registry.h` has the same shape.

**Three probes, each with a different blind spot, and the published figure is
their union.** Over `git ls-files include src tests`:

| Probe | What it does | Blind spot | `55596792` | `e24d1b24` |
|---|---|---|---|---|
| A, line-scoped | `grep -rlE 'Ported from.*<rev>'` | any header that wraps | 90 | 247 |
| B, leading comment block | strip comment leaders from line 1 until the block ends, join, then match | a header that is not the file's FIRST comment block (5 such files) | 156 | 317 |
| C, whole file, 1200-char window | strip every comment leader, join, match `<rev>` within 1200 chars after a `Ported from` | a header that lists enough paths to push the revision past the window (2 such files) | 175 | 330 |
| **union of A, B, C** | | **unknown, hence a floor** | **177** | **330** |

B is not a superset of A and C is not a superset of A or B, which is why the
answer is a union rather than the largest single probe. 556 files carry a
`Ported from:` header at all (probe C's `any` bucket).

**What the floor says.** At least 177 files go behind with this advance, on top
of at least 330 that were ALREADY behind against the PRIOR pin — this wave
neither causes nor fixes those, and their existence says the per-file pin has
been drifting for two cycles, not one. Exactly one file's header names the new
pin.

**The lesson this section owes the next reader is not "grep harder".** A
line-scoped probe over a wrapped record fails toward a LOW number and reads as a
clean measurement. Both times this wave got it wrong, the wrong answer looked
exactly like a right one. Pair every count with a positive control through the
same probe form, and where the record wraps, normalise before you match.

`../upstream-sync.md` §Concepts allows a per-file pin to be ahead and requires a
ledger note for one that is behind. **This section and `../upstream-sync.md`'s
own Active-pin paragraph are that note.** Owed by
[#2611](https://github.com/mudler/vllm.cpp/issues/2611).

### 5.4 The published binding ratios were measured against the previous pin

Five rows across `docs/benchmarks/vllm-online-serving.md` and
`docs/benchmarks/speculative-decoding.md` carry a vLLM denominator measured at
`555967922` with FlashInfer `0.6.15.post1`. They keep their numbers and gain a
statement of that fact and of the owed re-measurement (#2818), on the pages
themselves and in `docs/benchmarks/how-we-measure.md`. **Nothing is withdrawn**,
and nothing now reads as though it were measured at the current pin.

### 5.5 Two discharges argued at the OLD pin, re-stated

Both hold at the new one, because neither argument depends on which revision is
pinned, and both keep their original scope limits from STEP6 §4 and §5:

- **The `transformers` floor** moved `>= 5.5.3` to `>= 5.10.4`. The resolved
  version is `5.14.1`, above both. It is a LOWER bound and it is satisfied. What
  this does not cover, and STEP6 said so: a rebuild of the oracle environment
  resolves the open bound to whatever is newest that day (`5.16.1` was measured
  at the target). That belongs to golden-drift re-validation, not to this
  denominator.
- **`VLLM_ALLREDUCE_USE_FLASHINFER`** flips `False` to `True` (vllm#52998,
  `6a962071bd`). It is inert at `tensor_parallel_size == 1`, and every committed
  gate runs 1. If a gate ever runs TP > 1, this discharge is void and the flag is
  back on the list.

**`nvidia-cutlass-dsl` is NOT re-stated as discharged**, because it is not one.
STEP6 §3 withdrew that discharge after a fresh review found
`vllm/model_executor/warmup/kernel_warmup.py:125-126` gated on
`has_device_capability(90)` — a `>=` test, which admits `sm_121`, unlike the
`is_device_capability_family(100)` tests the first pass generalised from — whose
`compile(...)` is unguarded. It rides with #2818 as `C1c`.

## 6. Gates, each run and each rc read directly

1. `python3 scripts/check-oracle-pins.py` — the advanced `oracle-pin` block
   parses, `gateable = yes` resolves its evidence to a path that exists, the
   registry still names exactly one primary.
2. `python3 scripts/check-now-current.py` — `.agents/NOW.md` after the pin line
   was corrected.
3. `python3 -m unittest tests.tools.test_oracle_pin` — `read_parity_pin` accepts
   the new block and `assert_oracle_commit` accepts both new version strings
   while still refusing the rollback, a commit-less version and a foreign commit.
   These assertions are value-agnostic by construction, which is why they are the
   ones that can catch a malformed advance.
4. `python3 scripts/check-symbol-anchors.py --upstream-root` before and after,
   §4.4.
5. `scripts/agent-preflight.sh --staged`, ONCE, on the frozen tree, with the
   `gate(s) failed` and `NOT a green` lines read rather than the exit status.

The outputs are in the pull request body.

## 7. Carry-over

**C1. Step 6 is owed, POST-HOC**
([#2818](https://github.com/mudler/vllm.cpp/issues/2818)). Five rows plus `C1c`,
all on `dgx:gpu0`, on the recipe
[`2026-09-03-e126687-step6.md`](2026-09-03-e126687-step6.md) §7 carries. **A red
means revert the pin.**

**C2. The declared token-exact gate is owed**
([#2794](https://github.com/mudler/vllm.cpp/issues/2794)), on `dgx:gpu0`, and it
also discharges the OPT golden that was never re-validated at the prior pin. It
is NOT subject to the harness refusal §1 describes, so it can run at any time.

**C3. A reading on `dgx:gpu0` is owed**
([#2611](https://github.com/mudler/vllm.cpp/issues/2611)), with the 290-entry
queue and the >= 177 file headers §5.3 leaves behind the pin.

**C4. The queue's discharge is in an unmerged pull request** (#2768). §3.

**C5. `qwen4_exp` still does not run on this fleet**
([#2626](https://github.com/mudler/vllm.cpp/issues/2626)). The advance improves
that row's position — the architecture is registered at the pin now, where before
it was not — and does not fix it.

**C6. #2649 is unblocked and not landed**
([#2649](https://github.com/mudler/vllm.cpp/issues/2649)). §4.3. Until it lands
the tree disagrees with its own pin at
`include/vllm/config/speculative.h`.

**C7. The model matrix is unreconciled in both directions**
([#2819](https://github.com/mudler/vllm.cpp/issues/2819)). §4.4.

**C8. A queued token-gate job is UNAFFECTED by this change.** `rc` job
`efc30c74-005e-4e80-bc28-bd34f5b76b77`, queued on `dgx:gpu0` at position 12 by
another session, already targets `e126687a9a`. Its result lands under its own
change and neither depends on nor is invalidated by this one. **This wave held no
lease, requested none, and did not touch that job.**

**C9. Nothing was executed on a device, and one thing was executed off it.** The
checkers in §6, and `git`/`git grep` against a local vLLM checkout containing
both revisions. No build, no lease, no GPU, no network call other than the GitHub
issues this report names.

**C11. The gateability table holds ZERO rows measured at this pin**
([#2794](https://github.com/mudler/vllm.cpp/issues/2794) is the obvious first
one). `../oracles/vllm.md` §"Device-scoped gateability" carried one `strix:gpu0`
row measured at the PRIOR pin — its evidence file records
`SETUPTOOLS_SCM_PRETEND_VERSION=0.26.0.dev0+g5559679229` at `:246` and the same
string in the engine banner at `:340`. The row is KEPT with its pin column added,
because deleting a real measurement of a real board would destroy evidence, and
the section now states outright that the table is empty at the active pin.
`thor:gpu0` was deliberately NOT added: `facebook/opt-125m` is not a gate model,
and a row for it would make the table say the thing this advance did not buy.

**C12. The two pin surfaces are not checked against each other**
([#2829](https://github.com/mudler/vllm.cpp/issues/2829)). Pre-existing, made
live by this advance. With `../upstream-sync.md` at the new pin and
`../oracles/vllm.md` rolled back to `5559679229` in a scratch mutation,
`check-oracle-pins.py`, `test_oracle_pin` (25 tests) and `check-agent-record.py`
all returned rc 0. The tree was restored afterwards. `check-oracle-pins.py`
declines the comparison in its own docstring, on the correct ground that a second
transcription is the defect; the issue records that the fix is to DERIVE the
value, not to add a checker that syncs two copies.

**C10. Prior-pin line anchors into `../oracles/vllm.md` are now wrong.**
[`2026-09-03-e126687-step6.md`](2026-09-03-e126687-step6.md) and
[`../specs/upstream-sync-headpin-step6.md`](../specs/upstream-sync-headpin-step6.md)
cite `.agents/oracles/vllm.md:66-69`, `:58-70` and `:70`, and this wave rewrote
that section. They are NOT edited: they are past-wave records and they described
the file as it was. This is the #1143 shape, and it is the reason
`check-symbol-anchors.py` exists and the reason it cannot verify a bare line
number.
