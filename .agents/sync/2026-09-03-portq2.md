# PORT-NOW tranche 2 of `5559679229..e126687a9a`, re-derived against this tree

Wave PORTQ-2. Row `UPSTREAM-SYNC-HEADPIN`.
Issue: [#2646](https://github.com/mudler/vllm.cpp/issues/2646).
Spec: [`../specs/upstream-sync-portq2.md`](../specs/upstream-sync-portq2.md).
Tree read: `origin/main` at `e24ec8bfd`.
Upstream read at revision, never in a working tree.
Predecessor: [`2026-09-03-portq1.md`](2026-09-03-portq1.md), entries 1-40.

## 1. The answer

Of PORT-NOW entries **41 through 80** of `5559679229..e126687a9a` in upstream
commit order:

| | Count |
|---|---:|
| ALREADY_SATISFIED | **4** |
| REAL_GAP | **9** |
| NOT_APPLICABLE | **27** — 23 `surface-absent`, 4 `inert` |
| **Disagree with the recorded disposition** | **31 of 40** |

`PORT-NOW` asserts that portable work exists here, so it holds exactly where the
label is `REAL_GAP` and nowhere else. Thirty-one of these forty do not hold.

**Nothing in this wave was executed.** No build, no test run, no GPU lease. Every
label is a static reading of source. `ALREADY_SATISFIED` here means the code
implements the behaviour, **not** that a gate observed it doing so.

**Nine real gaps is half again PORTQ-1's six**, and §7 says why that is an
observation about two contiguous slices rather than a rate.

**Four of the forty had already been re-derived against this tree**, by #2524's
stratified sample, and are listed by name in
[`2026-09-01-cdefd9d.md`](2026-09-01-cdefd9d.md) §13: `[55]`, `[60]`, `[73]`,
`[74]`. All four were re-verified at `e24ec8bfd` rather than carried, **all four
still hold, and no anchor had gone stale.** §6 records what re-verification added
to two of them.

The nine gaps and the four deferred entries each carry an issue naming an owning
row that resolves in `roadmap_v1.md` or a `*-matrix.md`; §4 and §5 list them.

## 2. Method

### 2.1 The tranche reproduces two committed denominators before it is used

```console
$ git rev-list 5559679229..e126687a9a | cut -c1-10 | sort -u | wc -l          # 1465
$ comm -12 <(sort portnow.txt) range.txt | wc -l                              # 290
$ git rev-list --reverse 5559679229..e126687a9a | cut -c1-10 \
    | grep -x -F -f inrange.txt | sed -n '41,80p'
```

`portnow.txt` is the 315 SHAs of [`2026-09-01-cdefd9d.md`](2026-09-01-cdefd9d.md)
§4. `1465` and `290` are the counts [`2026-09-02-e126687.md`](2026-09-02-e126687.md)
§4 publishes. A third check ties this tranche to the previous one: offsets 1 and
40 of the same ordering come out `3f1d40960f` and `b49eaf205a`, which are exactly
PORTQ-1's published first and fortieth entries.

### 2.2 The labels are PORTQ-1's, deliberately, so the tranches compose

`ALREADY_SATISFIED` cites the `file:line` or symbol. `REAL_GAP` says what is
missing, roughly how big, and who owns it. `NOT_APPLICABLE` splits into
`surface-absent` (discarded work) and `inert:<gate>` (deferred work; the gate is
named, and the entry gets an owner).

### 2.3 Absence was searched twice, in this tree's vocabulary

PORTQ-1's retraction is this wave's binding lesson: six greps for upstream
identifiers returned zero and were read as absence, while seven of them existed
here under other names. Every claim of absence below names the searches that
returned nothing, and each was searched for the CONCEPT and not only the upstream
spelling. Two of this tranche's readings turn on exactly that: `[69]`'s deepstack
allocation is data-driven rather than flag-driven, and `[66]`'s "no torch thread
management" would have been the whole answer if the concept — how many CPUs this
process believes it has — had not been searched separately.

**The rule, stated as a rule: pair every zero with a positive control run
through the SAME probe form.** A zero from a malformed pattern is
indistinguishable from a zero from an absent symbol, and nothing downstream can
tell them apart. This wave's operator pass produced one: `grep -rniE "a\|b"`
treats `\|` as a LITERAL pipe under ERE, so a nine-term probe returned `0` for
`VLLMValidationError|InputValidationError` on a tree that carries
`InputValidationError` at `include/vllm/v1/engine/validation_error.h:42`. The
fresh review of this change produced the same failure twice with a different
mechanism — an unquoted `--include=*.h` eaten by zsh globbing, returning silence
at rc 0 — and caught both by the control. The incident is common; the control is
the method.

**Every zero-hit list below is scoped to `include/` and `src/` unless it names a
wider scope**, and §3 says so where the distinction matters. An unstated scope is
what makes a zero unfalsifiable.

### 2.4 Git and the committed deferral text were asked first

`git log --oneline --grep '<sha>'` and `--grep 'vllm#<PR>'` over `origin/main`,
plus a grep of the tree and `.agents/` for both, for all forty. **No commit
message references any of the forty**, and the only `.agents/` hits are the sync
records themselves — except the four in §6, which `2026-09-01-cdefd9d.md` §13
had already worked.

Shape B — "recorded in this repository as deliberately absent" — again settled
most of the tranche from text already committed here, before any upstream diff was
opened.

### 2.5 The reading was delegated to four readers, and its citations re-read

Ten entries each, from one shared brief. This wave's operator independently
derived sixteen of the forty before the readers returned, and printed and checked
every `file:line` behind all nine real gaps and both prior-triage re-verifications.
**No citation named a symbol that does not exist, and no anchor resolved to
something other than what was claimed.** One label was adjudicated against its
reader; §5 records it as a disagreement rather than absorbing it.

## 3. The forty

Ordered oldest first. **"Holds?"** is whether the recorded `PORT-NOW` disposition
holds for this tree; it is `yes` exactly on `REAL_GAP`.

| # | upstream | vllm# | label | holds? | evidence |
|---:|---|---|---|---|---|
| 41 | `1180b604f8` | 49686 | NA **inert**: no second hash algorithm exists | no | `MultiModalHasher` hard-codes BLAKE3 in both entry points (`include/vllm/multimodal/hasher.h:20`), and `:8` names it "vLLM's default `VLLM_MM_HASHER_ALGORITHM`" — the pre-commit env selector was never ported either. `mm_hasher_algorithm`, `sha256`, `sha512` → 0 hits inside `include/vllm/multimodal` and `src/vllm/multimodal`; `third_party` carries blake3 only. A selector with one choice is dead code. [#2665](https://github.com/mudler/vllm.cpp/issues/2665). |
| 42 | `10e6b40015` | 50437 | NA surface-absent | no | The commit edits `CPUAttentionBackendImpl.forward`. `src/vllm/v1/attention/backends/cpu_attn.cpp` is a 39-line registrar with no impl class; `include/vllm/v1/attention/backend.h:15` records the impl factories as "Task 3". The KV write is issued once per layer by each model (`src/vllm/model_executor/models/granite.cpp:134`), device-agnostically, so there is no duplicate to remove. `CPUAttentionBackendImpl`, `cpu_attn_reshape_and_cache` → 0. |
| 43 | `0b5b49dde8` | 50491 | NA surface-absent | no | The exception TYPE is here — `include/vllm/v1/engine/validation_error.h:42` `InputValidationError`, documented as this tree's `VLLMValidationError` — but none of the four call sites the commit converts is ported. Only `_validate_chat_mm_limits` is mirrored (`src/vllm/entrypoints/openai/chat_mm.cpp:268`). `merge_embeds`, `enable_prompt_embeds` → 0; `image_embeds` is a parsed-but-empty content part recorded as a residual at `include/vllm/entrypoints/openai/protocol.h:384-385`. |
| 44 | `82ae4164ee` | 48770 | NA **inert**: no sparse-MLA prefill backend registers | no | `_use_masked_mha` keys on `FLASHMLA_SPARSE` / `FLASHINFER_MLA_SPARSE`, which exist only in the priority table (`include/vllm/platforms/cuda_attn_priority.h:82,131,133`); `include/vllm/v1/attention/registry.h:82` says "Today the sparse entry is unregistered". Second gate: family-100 + FA4, recorded unreachable on sm_121 at `.agents/kernel-matrix.md:140`. [#2666](https://github.com/mudler/vllm.cpp/issues/2666). |
| 45 | `df71917cf1` | 49236 | NA surface-absent | no | `eager_scratch`, `fused_deepseek_v4`, `fused_indexer_q`, `qnorm_rope` → 0. The FP4 indexer arm those kernels serve is recorded owed at `src/vllm/model_executor/models/deepseek_v4_registry.cpp:396-399`. Worth recording: the post-commit INTENT is already this tree's design — "persistent unified scratch (allocated once; never resized -> stable addresses)" at `src/vllm/model_executor/models/deepseek_v4.cpp:2628`, used with `vt::BreakableGraph` at `:2646`. |
| 46 | `94e9ef0768` | 50137 | NA **inert**: BF16-only refusal precedes the orientation test | no | `src/vllm/model_executor/models/qwen3_5_weights.cpp:965-977` refuses any non-BF16 stacked expert by name, upstream of the branch, so a fused quantization SCALE can never reach it. **Both halves are SATISFIED rather than absent, positively**: `:988-993` is `_orient_fused_weight`'s post-commit rule already — `gu.shape[2] == hidden`, else `gu.shape[1] == hidden`, else refuse by name — and `hidden_dim_unpadded or hidden_dim` is this tree's behaviour by construction, there being no padded-hidden concept to be unset. So the entry is inert on the quantized arm, not on the rule. [#2667](https://github.com/mudler/vllm.cpp/issues/2667). |
| 47 | `a0cd2b69b3` | 50302 | **REAL_GAP** | yes (part) | `src/vllm/v1/worker/gpu/block_table.cpp:210-216` is the pre-commit `mult = 128 / bs` alignment verbatim, cited to the same upstream issue (#39324), reached from `src/vllm/v1/worker/gpu/input_batch.cpp:105,123`. The gcd form differs for any block size that does not divide 128. The Mamba opt-out half is surface-absent: `SlotMappingMode` → 0. [#2656](https://github.com/mudler/vllm.cpp/issues/2656). |
| 48 | `8d8a4e0f2b` | 50515 | NA surface-absent | no | The commit repairs what the ParserEngine unification broke; Mistral was never unified here (`src/vllm/parser/` holds gemma4, glm47_moe, inkling, kimi_k2, qwen3). The legacy parser already exposes the marker publicly: `static constexpr const char* kBotToken = "[TOOL_CALLS]"` at `include/vllm/entrypoints/openai/tool_parsers/mistral.h:75`. `bot_token_id` has no surface by design, recorded as deviation D2 at `:24-27`. |
| 49 | `e8b358b4c0` | 50334 | NA surface-absent | no | There is no Responses API and no serializer to patch: `ChatCompletionToolsParam` has one conversion, `from_json` at `src/vllm/entrypoints/openai/protocol.cpp:505`. The two fields the commit's serializers pop are both deferred (`include/vllm/entrypoints/openai/protocol.h:123,132`). `iter_response_function_tool_dicts`, `v1/responses` → 0. |
| 50 | `e3be89673d` | 50019 | NA surface-absent | no | There is no `QuantizationConfig` and no capability-admission hook: `include/vllm/model_executor/layers/quantization/base_config.h` is 51 lines and defines only `QuantizeMethodBase`. `get_min_capability`, `MinCapability` → 0. The nearest concept IS here and is untouched by the commit: `CudaPlatform::supports_fp8()` mirrors `cuda.py:562` at `src/vllm/platforms/cuda.cpp:47`. |
| 51 | `6c91de3689` | 50642 | NA surface-absent | no | The `Parser` composite the commit edits is not ported. The reasoning-parser seam here is a NAME-ONLY factory over an ABC with a default constructor (`include/vllm/entrypoints/openai/reasoning_parsers/abstract.h:94`, `:55-56`; the deviation is argued at `:14-30`). `class Parser`, `reasoning_parser_cls`, `ModelConfig` under the parser trees → 0. |
| 52 | `dc818c198d` | 45560 | NA surface-absent | no | `harmony` has zero hits in any `.h`/`.cpp`; there is no gpt-oss parser, no `/v1/responses`, and `XGRAMMAR_BUILTIN_STRUCTURAL_TAG_MODELS` is not mirrored as data — only as prose at `include/vllm/entrypoints/openai/tool_parsers/structural_tags.h:58-59,66`. The model is `INVENTORIED` (`.agents/model-matrix.md:247`). |
| 53 | `b3f97dae24` | 50816 | NA surface-absent | no | `cache_salt` is an explicitly DEFERRED request field: `include/vllm/entrypoints/openai/protocol.h:37`. It exists engine-side only (`include/vllm/v1/engine/types.h:97`, `include/vllm/v1/request.h:162`), so there is neither the validator the commit deletes nor a schema to give a `min_length`. The consumer already treats an empty salt as absent (`src/vllm/v1/core/kv_cache_utils.cpp:509-510`). |
| 54 | `c8602c7906` | 50801 | NA surface-absent | no | Ten spellings, zero hits each **in `include/` and `src/`**: `sgl_kernel`, `weight_packed_linear`, `VLLM_CPU_SGL_KERNEL`, `dispatch_cpu_unquantized_gemm`, `check_cpu_sgl_kernel`, `zentorch`, `onednn`, `convert_weight_packed`, `AMX`, `CPUExperts`. `AMX` is not zero repository-wide — `scripts/check-windows-portability.py:1141-1142` names it in a refusal contract — which is a Windows packaging rule, not a CPU GEMM path. The CPU GEMM here is a hand-written ISA ladder (`src/vt/cpu/cpu_matmul_elem_{avx2,avx512,f16c}.cpp` via `src/vt/cpu/cpu_isa_x86.cpp`), not a torch dispatch table. |
| 55 | `68ca6fd02c` | 50869 | **REAL_GAP** | yes | The refusal is live at `include/vllm/config/speculative.h:364-375` and this tree WIDENED it — `src/vllm/entrypoints/model_loader.cpp:925-929` falls back to `block_size` when `dspark_block_size` is absent, and `:868-878` records that neither published Qwen3 draft carries the latter. Configurations that start upstream hard-fail here. §6.1 corrects how the ordering hazard reads at the target head. [#2657](https://github.com/mudler/vllm.cpp/issues/2657). |
| 56 | `dd11df04f3` | 49389 | ALREADY_SATISFIED | no | The runtime calibration path was never ported, so the post-commit state already holds: no config field (`include/vllm/config/cache.h:2` mentions it only in a header comment), no CLI flag, and `ResolveKvCacheScales` (`include/vllm/model_executor/layers/quantization/kv_cache.h:94-108`) guards on `IsQuantizedKvCache` alone and refuses the dynamic arm by name at `:106-108`. A vestigial `bool` parameter survives; see §8. |
| 57 | `f0de1a604c` | 50823 | NA surface-absent | no | `UniformTypeKVCacheSpecs` is in the omitted list at `include/vllm/v1/kv_cache_interface.h:48`; only its predicate half is mirrored, as `are_uniform_kv_cache_specs` (`include/vllm/v1/kv_cache_spec_registry.h:64-68`). The width the commit overrides is the base `cdiv` at `src/vllm/v1/worker/gpu/block_table.cpp:200-202`, whose own comment says `total_cp_world_size == 1 at T0`. DCP is inert besides: `src/vllm/v1/core/sched/scheduler.cpp:280` hard-codes `dcp_world_size=1`. |
| 58 | `7f2e78ba4d` | 49056 | NA surface-absent | no | Only the DECODE half is ported, and says so: `include/vllm/entrypoints/openai/chat_mm.h:7-8`. There is no base64 ENCODER in the product: `EncodeBase64`, `base64_encode`, `Base64Encode` → 0 in `include/` and `src/`, while `DecodeBase64` / `DecodeDataUri` exist (`chat_mm.h:57-60`). Three encoders DO exist test-locally (`tests/vllm/entrypoints/openai/test_chat_mm.cpp:77` and two API-server tests), which is the opposite of a counter-example: the fixtures build the data URLs the product only ever consumes. The media-emitting endpoints return raw bytes with a Content-Type header (`include/vllm/entrypoints/openai/video_api.h:10`), never a data URL. |
| 59 | `4a3447d200` | 50417 | NA surface-absent | no | `include/vllm/model_executor/models/interfaces.h:6-9` scopes that file to "the TOWER-SKIP decision, and nothing else … deliberately absent rather than stubbed". The draft proposer is a next-token-LOGITS oracle with no embed seam (`include/vllm/v1/spec_decode/draft_model_proposer.h:44-52`), and nothing under the spec_decode trees allocates `inputs_embeds`. No draft model here is multimodal. |
| 60 | `f57123aa2d` | 48048 | **REAL_GAP** | yes | `session_id` / `SessionId` / `X-Session-ID` → 0 across `include/`, `src/`, `tests/`; the concept search agrees — the request carries only `request_id` and `priority` (`include/vllm/v1/engine/types.h:77,88`) and the server discards the header-bearing object at `src/vllm/entrypoints/openai/api_server.cpp:1176-1179`. Every surface the commit touches IS ported, which is what makes it portable work. [#2658](https://github.com/mudler/vllm.cpp/issues/2658). |
| 61 | `0b37d8389f` | 48861 | **REAL_GAP** | yes | The per-tensor fp8 QKV emits f32 where upstream emits the model dtype: `src/vllm/model_executor/models/qwen3_5.cpp:2835-2839` passes `DType::kF32` on both fp8 arms, while the bf16 arm below and the fp4 arm (`:2815-2816`) emit `kBF16`. Already recorded as a deliberate deviation at `.agents/specs/model-fp8-block-linear.md:133`; what this commit adds is upstream authority for the rule. A token gate cannot see it. [#2659](https://github.com/mudler/vllm.cpp/issues/2659). |
| 62 | `c2881ce603` | 50432 | NA surface-absent | no | The worker-side V2 align postprocess has no analogue: `postprocess_mamba`, `aligned_new_computed`, `num_accepted_out`, `state_idx_gpu` → 0, and no file under `src/vllm/v1/worker/gpu/` touches mamba state. `.agents/engine-matrix.md:101` already records this as `KV-MAMBA-ALIGN` item (2). Deferred, not discarded: the warning rides on [#2660](https://github.com/mudler/vllm.cpp/issues/2660). |
| 63 | `6a9fdf0dc7` | 50950 | NA surface-absent | no | There is no `adapters.py` analogue and no config-sourced `num_labels`: every occurrence is the pooler ACTIVATION parameter (`include/vllm/model_executor/layers/pooler/activations.h:52-64`), which falls back to the row width at `src/vllm/model_executor/layers/pooler/activations.cpp:52` and is never read from an HF config. `as_seq_cls`, `_resolve_num_labels`, `score_head` → 0. |
| 64 | `199644d410` | 50906 | NA surface-absent | no | Only the `use_dense_mha` half of `sparse_mla_attention.py` was ported; the route at `src/vllm/model_executor/layers/attention/mla_attention.cpp:478-500` is dense-versus-per-token-MQA with no third arm. `GLOBAL_TOPK_MASK`, `masked_mha`, `masked_mha_workspace_fits`, `topk_mask`, `mask_workspace` → 0. There is no buffer for the commit's bound to bound. Carried with [44] on [#2666](https://github.com/mudler/vllm.cpp/issues/2666). |
| 65 | `f9c74b4b9c` | 50991 | **REAL_GAP** | yes (part) | The exclusion is verbatim: `src/vllm/entrypoints/model_loader.cpp:1355` returns `!model_info.is_hybrid && !model_info.has_inner_state`. The commit's second half has no resolver — every registry builds `MambaSpec` with `"none"` (`src/vllm/model_executor/models/qwen3_5_common.cpp:89` explicitly; `include/vllm/v1/kv_cache_interface.h:469` by default elsewhere). The one-line flip is not landable alone. [#2660](https://github.com/mudler/vllm.cpp/issues/2660). |
| 66 | `7635a9002b` | 49919 | **REAL_GAP** | yes (part) | Two halves are surface-absent (no torch, no multiproc executor — `include/vllm/v1/executor/executor.h:41-47`). The third is not: `ThreadsFromEnv()` falls back to `std::thread::hardware_concurrency()` (`src/vt/cpu/cpu_threadpool.cpp:111-120`), which reports HOST cores in a cpu-limited container. `cgroup`, `cpu.max`, `cpuset`, `sched_getaffinity` → 0. [#2661](https://github.com/mudler/vllm.cpp/issues/2661). |
| 67 | `41a7e7da0c` | 50321 | ALREADY_SATISFIED | no | The partial-failure case cannot arise: a promotion batch is submitted as one FS job PER KEY (`src/vllm/v1/kv_offload/tiering_manager.cpp:160-170`, `JobMeta` holds a single `key` at `:64-70`), and `finalize_one` (`:110-124`) completes each key with its own verdict — the post-commit `_complete_promotion` behaviour at a finer granularity. Structural, not a ported guard; see §8. |
| 68 | `eb3dce9757` | 50958 | NA surface-absent | no | There is no batched audio input to re-pad: `Gemma4AudioForward` takes ONE clip (`include/vllm/model_executor/models/gemma4_audio.h:140`; `src/vllm/model_executor/models/gemma4_audio.cpp:147-150` unsqueezes `[T,F]`), with no batch dimension and so no `squeeze(1)` to replace. `gemma4_mm.cpp` contains zero occurrences of "audio". Gemma3n does not exist here. |
| 69 | `7794b1e08b` | 49397 | ALREADY_SATISFIED | no | The buffers the commit stops allocating do not exist in any arm. DeepStack rides inside the per-step encoder output and is unpacked model-side (`src/vllm/v1/worker/gpu/runner.cpp:2037-2042,2558`; `src/vllm/model_executor/models/qwen3_vl_registry.cpp:135-157`); the consumer is data-driven (`src/vllm/model_executor/models/qwen3_vl.cpp:339`, `has_ds = deepstack.data != nullptr && Numel() > 0`). The tower-skip arm IS ported (`qwen3_vl.cpp:463-468`) and allocates nothing per level. |
| 70 | `b92352ca2c` | 50992 | **REAL_GAP** | yes | The pre-commit quadratic shape exactly: `ARCCachePolicy::evict` loops n rounds (`src/vllm/v1/kv_offload/cache_policy.cpp:257-286`) and each round's `pick()` restarts `from.keys_in_order()` at the LRU end, skipping an `already_selected` set (`:322-337`). O(n·|T1| + n·|T2|). [#2662](https://github.com/mudler/vllm.cpp/issues/2662). |
| 71 | `cd930c8e78` | 38771 | NA surface-absent | no | There is no quantized `kv_b_proj` arm, so there is no activation dtype to resolve: both apply sites feed a bf16 GEMM (`src/vllm/model_executor/layers/attention/mla_attention.cpp:355`, `:998`) and the absorption helper pins the dtype in its signature (`AbsorbKvBProjBf16`, `:224`). Marlin itself is heavily ported (1218 hits); it simply never reaches `kv_b_proj`. |
| 72 | `66b3c0e61f` | 50294 | NA surface-absent | no | `mm_prefix` exists only as a backend-selection refusal: declared at `include/vllm/platforms/interface.h:180`, checked once at `src/vllm/v1/attention/backend.cpp:170`, and `supports_mm_prefix()` returns false on the base and is overridden nowhere (`include/vllm/v1/attention/backend.h:373`). `mm_prefix_range`, `mask_mod`, `PrefixLM` → 0. §8 records that no row owns the feature. |
| 73 | `23c0f337b7` | 51180 | **REAL_GAP** | yes | `src/vllm/v1/core/kv_cache_utils.cpp:679-682` still compares `m->block_size != cache_block_size`; the field the fix switches to exists at `include/vllm/v1/kv_cache_interface.h:481` and is read at `src/vllm/v1/core/single_type_kv_cache_manager.cpp:752,788,818,880`. The red-first hook is already committed; see §6.2. [#2663](https://github.com/mudler/vllm.cpp/issues/2663). |
| 74 | `e76b71f659` | 51179 | **REAL_GAP** | yes | Sharper than "one line, latent": `GetInt` (`src/vllm/transformers_utils/hf_config.cpp:22-26`) is `it->get<int64_t>()` with no array branch, so the list-valued key at `:458` throws `json::type_error`, caught at `:593-595` and rethrown as "hf_config: bad field type". The checkpoint does not load at all. `moe_topk` / `moe_top_k` are unread besides. [#2664](https://github.com/mudler/vllm.cpp/issues/2664). |
| 75 | `482c613423` | 51093 | NA surface-absent | no | The commit tags dim ROLES on a torch `Parameter`. There is no such machinery: `BasevLLMParameter`, `ModelWeightParameter`, `weight_loader` as a symbol → 0, and the `output_dim`/`input_dim` hits are unrelated local config widths. There is no `ModelOptFp8LinearMethod`; the fp8 apply seam is `include/vllm/model_executor/models/dense_fp8_gemm.h:27`, which ports `apply`, not the parameter tagging. |
| 76 | `7c77868cdf` | 51125 | NA surface-absent | no | No `FusedMoEConfig`, so no `is_act_and_mul` and no `w13_num_shards` — all three hits are comments quoting upstream, at `src/vllm/model_executor/models/qwen3_5.cpp:997,6778` and `tests/vt/test_ops_moe_grouped.cpp:716`. MoE expert layouts are per-model loader code. The one non-gated MoE here, NemotronH's relu² experts, never allocates a gate half: `ckpt_names=("up_proj","down_proj","")` at `src/vllm/model_executor/models/nemotron_h_device.cpp:730`. |
| 77 | `47a4e410ba` | 50183 | ALREADY_SATISFIED (the OOB property) | no | **The defect the commit fixes — an out-of-range block index and the OOB read after it — cannot arise on either arm.** `ArgReduce` (`include/vt/sample_common.h:87-92`) is `if (bv > av \|\| (bv == av && bi < ai))` and carries the (value, index) PAIR through the reduction, so there is no `argmax(local_max)` → `load(local_argmax + idx)` indirection to go out of range; CUDA seeds a sentinel and collapses it to 0 (`src/vt/cuda/cuda_sample.cu:913-933`). **Scoped deliberately**: upstream's `where(NaN, -inf)` also makes a row of mixed NaN and finite logits select the FINITE max, and this tree does not — `src/vt/cpu/cpu_sample.cpp:330-331` seeds `best_v = r[0]` under a strict `>`, so a NaN at index 0 is never displaced and the row returns 0 with a finite max present; CUDA has the same polarity. That is a NaN-input selection difference, not the index-range bug, and it is out of this entry's scope. The second hunk's resample kernel is deferred by name (`include/vllm/v1/spec_decode/rejection_sampler.h:53-57`). |
| 78 | `8cfa01cdd2` | 49212 | NA surface-absent | no | There is no multinode DP and no `ParallelConfig` to rescope: `include/vllm/distributed/parallel_layout.h:37-45` is the whole DP surface, a value used only for rank arithmetic, with no `nnodes` or `node_rank` and nothing mutating it. `reconfigure_for_independent_dp_rank`, `nnodes`, `node_rank` → 0 in `include/` and `src/`; `DPEngineCore` is zero as a SYMBOL and non-zero as prose — `include/vllm/v1/engine/core_proc.h:270` names `DPEngineCoreProc` in the comment cited beside it. `include/vllm/v1/engine/core_proc.h:270` records the DP-wave variant as not taken. |
| 79 | `b50fdebce0` | 39935 | NA surface-absent | no | Sleep mode does not exist: `include/vllm/v1/executor/executor.h:23` and `include/vllm/v1/engine/llm_engine.h:50` both list `sleep` / `wake_up` as not implemented. `CuMemAllocator`, `reload_weights`, `is_sleeping` → 0. The LoRA half is equally absent — `BaseLayerWithLoRA`, `LoRAModelManager`, `set_mapping` → 0; LoRA here is a CPU brick with a per-slot `reset_lora` (`include/vllm/lora/layers.h:107`) and no manager. |
| 80 | `81bc196913` | 50910 | NA **inert**: probabilistic draft sampling is refused by name | no | `src/vllm/config/speculative.cpp:489-496` refuses any `draft_sample_method` but `"greedy"`, and the message names the owing row. The cache the commit re-dtypes exists upstream only under probabilistic drafting and is consumed only by the stochastic rejection sampler; both are deferred here (`include/vllm/v1/worker/gpu/spec_decode/dflash2/speculator.h:13`, `include/vllm/v1/spec_decode/rejection_sampler.h:53-57`). [#2668](https://github.com/mudler/vllm.cpp/issues/2668). |

## 4. The nine real gaps

| # | upstream | what is missing | size | owner | issue |
|---|---|---|---|---|---|
| 55 | `68ca6fd02c` | the general DSpark `k >= block_floor` refusal upstream deleted is live here, and widened to fall back to `block_size`, so it fires on both published Qwen3 drafts | ~200-250 lines, mostly deletions, over ~6 files | `SPEC-DSPARK-BLOCK-SIZE-GUARD` | [#2657](https://github.com/mudler/vllm.cpp/issues/2657) |
| 61 | `0b37d8389f` | the per-tensor fp8 QKV projection emits `kF32` where upstream emits the model dtype — the too-wide-dtype class a token gate cannot see | ~15-40 lines over 1-2 files, plus a memory-format assertion | `QUANT-FP8-GENERIC` | [#2659](https://github.com/mudler/vllm.cpp/issues/2659) |
| 74 | `e76b71f659` | a list-valued `num_experts_per_tok` makes the whole `hf_config` parse throw, so the checkpoint does not load | ~10-14 lines over 2 files | `LOAD-CONFIG-SURFACE` | [#2664](https://github.com/mudler/vllm.cpp/issues/2664) |
| 73 | `23c0f337b7` | the mamba back-off predicate compares block sizes instead of reading `mamba_cache_mode` | ~6 lines over 2 files; the red-first test is already committed | `KV-PREFIX-MATCH-UNIT` | [#2663](https://github.com/mudler/vllm.cpp/issues/2663) |
| 70 | `b92352ca2c` | ARC batch eviction restarts the LRU scan per candidate; O(n·\|T\|) where upstream is linear at identical selection order | ~25-35 lines in one file | `KV-OFFLOAD` | [#2662](https://github.com/mudler/vllm.cpp/issues/2662) |
| 66 | `7635a9002b` | the CPU thread count is `hardware_concurrency()`, blind to a cgroup quota — and this project's own lease model runs jobs in containers | ~60-90 lines over 1-3 files | `QUANT-GGUF-CPU-THREADPOOL` | [#2661](https://github.com/mudler/vllm.cpp/issues/2661) |
| 60 | `f57123aa2d` | `session_id` is not carried, and no handler reads request headers at all | ~150-220 lines over ~10 files; the header seam is the big part | `SERVE-COMPLETION-LONGTAIL` | [#2658](https://github.com/mudler/vllm.cpp/issues/2658) |
| 65 | `f9c74b4b9c` | prefix caching still defaults OFF for hybrid models; the flip is not landable without this row's other two items | the row, not the line | `KV-MAMBA-ALIGN` | [#2660](https://github.com/mudler/vllm.cpp/issues/2660) |
| 47 | `a0cd2b69b3` | block-table width aligns by `128 / block_size` rather than `128 / gcd(128, block_size)` | ~25-40 lines over 2 files | `ENG-RUNNER-MODELSHAPE` | [#2656](https://github.com/mudler/vllm.cpp/issues/2656) |

**Not one of the nine is a live defect on a path this tree runs today.** That is
the honest ranking, and each issue says so in its own words:

- `[55]` is the closest: it REFUSES a configuration upstream accepts, on
  checkpoints this project has. It is user-visible now.
- `[61]` and `[74]` are latent on reachable paths — an f32 QKV output that no
  token gate can see, and a checkpoint shape that throws on load.
- `[73]`, `[70]`, `[66]`, `[47]` are correctness or cost repairs whose symptoms
  need a configuration nobody currently selects.
- `[60]` is a port, not a bug. `[65]` is a default this row cannot flip alone.

## 5. The disagreements, and one adjudication

Thirty-one of forty, in the same three shapes PORTQ-1 named.

**Shape B — the surface is recorded, in this repository, as deliberately absent
— is again the majority.** `[42]` `backend.h:15`, `[43]` `interfaces`-scope,
`[49]` `protocol.h:123,132`, `[53]` `protocol.h:37`, `[57]`
`kv_cache_interface.h:48`, `[58]` `chat_mm.h:7-8`, `[59]` `interfaces.h:6-9`,
`[62]` `engine-matrix.md:101`, `[72]` `attention/backend.h:373`, `[78]`
`core_proc.h:270`, `[79]` `executor.h:23`, `[80]` `speculative.cpp:489-496`. The
committed deferral text settled each of these without opening a build.

**Shape C — this tree reached the post-commit state first (4).** `[56]`, `[67]`,
`[69]`, `[77]`. `[77]` is the clearest: upstream adds `where(NaN, -inf)` before a
two-level `tl.argmax`; this tree's reducer carries the (value, index) pair and
uses a strict `>`, so neither the NaN nor the out-of-range index can arise.
`[45]` is Shape C in spirit without being satisfiable — upstream's persistent
scratch pool is already this tree's DSv4 design, reached independently.

**One entry was adjudicated against its reader, and it is recorded rather than
absorbed.** `[41]` came back `REAL_GAP (part)` with a size of ~150-250 lines
"dominated by vendoring SHA-2". The facts were not in dispute; the label is. A
port whose bulk is work the commit does not contain, guarded by an algorithm this
tree does not implement, is `inert` under §2.2's own definition: the surface is
here, the new arm is gated on something absent from every configuration. It is
filed as deferred work with the gate named ([#2665](https://github.com/mudler/vllm.cpp/issues/2665))
rather than as a gap somebody could take today.

## 6. What re-verification of the four prior readings added

`2026-09-01-cdefd9d.md` §13 worked `[55]`, `[60]`, `[73]` and `[74]` at tree
`63889449c`. **All four still hold at `e24ec8bfd`, and no anchor had drifted.**
Two gained something.

### 6.1 `[55]`'s ordering hazard does not read the way carry-over C9 states it

C9 says `5789897aa4` ADDS the DSpark refusal, `68ca6fd02c` removes it and
`51def7848a` re-lands the removal, so SHA-ordered porting installs a validation
and deletes it.

At the **target head** the general refusal is indeed gone — but a narrower one
stands in its place. `_validate_qwen3_omni_dspark`
(`vllm/config/speculative.py:145-179` @ `e126687a9a`) requires
`num_speculative_tokens == block_size`, EQUALITY rather than `>=`, and is gated on
`_is_qwen3_omni_target`, so it does not reach a generic Qwen3 DSpark draft.

**And it reads the two keys in the OPPOSITE order from this tree, which an
earlier draft of this section got backwards.** Upstream at
`speculative.py:163-166` takes `block_size` FIRST and falls back to
`dspark_block_size`. This tree takes `dspark_block_size` first and falls back to
`block_size` (`src/vllm/entrypoints/model_loader.cpp:925-928`), and says so in as
many words at `:878` — "The explicit key still wins when a checkpoint does carry
it". Same key set, opposite precedence, so a draft config carrying BOTH with
different values resolves differently on the two sides. The earlier gloss —
"this tree's own fallback order, arrived at independently and now upstream's" —
is retracted: the fallback exists on both sides, and the order does not agree.

So the work is "delete the general refusal", not "delete the check"; whoever
takes [#2657](https://github.com/mudler/vllm.cpp/issues/2657) must read the target
head rather than this commit alone, and must decide the precedence question
rather than assume it is already settled.

### 6.2 `[73]`'s red-first test is already in the tree

`tests/vllm/v1/test_prefix_match_unit.cpp:174-180` builds its "align" arm as
`MakeMamba(16)` against `cache_block_size = 16`, and `MakeMamba` (`:43-47`) never
sets `mamba_cache_mode`, so it stays `"none"`
(`include/vllm/v1/kv_cache_interface.h:469`). Under upstream's corrected predicate
that arm backs off too, and `CHECK(hash_a != hash_n)` at `:180` goes RED. The
failing-for-the-right-reason test this port needs is already committed.

## 7. What this implies for the remaining 210 — an estimate, not a count

**9 of 40 is 22.5%.** PORTQ-1 measured 6 of 40 (15%) and #2524's stratified
sample measured 11 of 63 (17.5%). Pooling all three: **26 real gaps in 143
entries, about 18%.**

Applied to the 290: **roughly 45-60 real gaps. This is an estimate from three
samples, not a count of anything**, and four reasons say not to treat 22.5% as
the rate:

1. This tranche, like PORTQ-1's, is a **contiguous slice in commit order**, not a
   random draw. A period effect and a sample rate are not separable here.
2. Its population **overlaps #2524's worked list**: four of this tranche's nine
   gaps are #2524's own §13 items re-verified, so the three samples are not
   independent and pooling them overstates the evidence.
3. The four reader batches returned **2, 2, 4 and 2** gaps. At that spread one
   entry moves the figure by several points.
4. `[47]`, `[65]` and `[66]` are each `yes (part)` — a real gap in one of a
   commit's several halves. Counting a part as a whole inflates the rate against
   PORTQ-1, which had two such entries to this tranche's three.

**What is a count, and holds regardless:** these forty contain nine real gaps and
thirty-one labels that contradict the record.

## 8. What could not be determined

- **Nothing was executed.** No build, no test run, no GPU lease. Where an entry's
  evidence is a numerics or cost claim rather than a structural one — `[61]`,
  `[70]`, `[77]` — a differential run against the pinned oracle would say more
  than a citation does, and none was made. `[66]`'s case is the code plus the
  container fact in AGENTS.md; **no over-subscription was measured.**
- **`inert` is a reachability judgement.** `[41]`, `[44]`, `[46]` and `[80]` are
  called inert because no second hash algorithm, no registered sparse-MLA
  backend, no quantized stacked-expert arm and no probabilistic draft sampling
  exist *today*. Each gate is named so a later reader can falsify the claim by
  porting the gate; none was proven unreachable by construction.
- **`[61]`'s downstream acceptance is not proven.** Whether every consumer of the
  fp8 arm's q/k/v accepts bf16 was not traced. The fp4 arm already emits bf16
  under `Bf16GemmOutEnabled()`, which is strong but not conclusive;
  `.agents/specs/model-fp8-block-linear.md:304-306` names exactly this as a
  `NEEDS_DECISION`.
- **`[65]`'s ordering is not settled.** What a mode-`"none"` mamba group does
  under an enabled prefix cache here was not traced, and that is what decides
  whether the default flip or the runner-side state copy comes first.
- **`[67]` is satisfied structurally, with no gate to keep it that way.** If the
  FS tier ever batches several keys into one job — a plausible performance change
  — the gap reopens and nothing would catch it.
- **`[72]` surfaced a records gap this wave does not close.** No row in any matrix
  names `mm_prefix` / PrefixLM bidirectional attention, so the feature itself is
  unowned. Filing it belongs to whoever scopes that feature, not to a sync record.
- **The other 210 entries are untouched.** This wave neither confirms nor
  disputes any of them, and entries 81-120 belong to the sibling PORTQ-3 wave.

## 9. Two committed records the PIN ADVANCE will falsify, and one it does not

Neither of the two is stale today — each is correct at the current pin
`5559679229` — so neither is edited here. Each becomes wrong the moment the pin
moves past its commit, and the change that advances the pin owes both.

1. **`.agents/porting.md:78`** teaches that ModelOpt fp8's `out_dtype` is
   `torch.get_default_dtype()`. `0b37d8389f` (`[61]`) replaced that with
   `model_config.dtype`. This line is the guide a reader consults for precisely
   the too-wide-dtype check that entry is about.
2. **`calculate_kv_scales`'s `config/cache.py:111` anchor, in SIX places, one of
   them in product output.** `dd11df04f3` (`[56]`) deletes that line. The count
   is load-bearing because the pin-advancing change owes every one:
   `include/vllm/config/cache.h:2`;
   `include/vllm/model_executor/layers/quantization/kv_cache.h:90`;
   **`include/vllm/model_executor/layers/quantization/kv_cache.h:108`, inside the
   thrown refusal message, so a user sees it**;
   `tests/vllm/entrypoints/test_kv_cache_fp8_wiring.cpp:8` and `:680`; and
   `.agents/specs/fp8-kv-cache.md:1044`. (`fp8-kv-cache.md:46` names the key
   beside a `:76` anchor and is adjacent rather than a seventh instance.) An
   earlier draft of this section said "two", counting only the two `include/`
   comments and missing the one a user can read.

### 9.1 RETRACTED: `81bc196913` does NOT make the DFlash2 draft-logits record stale

An earlier draft of this section, and the body of
[#2668](https://github.com/mudler/vllm.cpp/issues/2668), claimed that `[80]`
`81bc196913` changes the recorded `(torch.float32, -inf)` draft-logits cache
layout to "the LM-head dtype holding pre-temperature logits", and that
`.agents/specs/dflash2-spec-decode.md` therefore goes stale on the advance.
**That is false, and acting on it would corrupt a correct record.**

At `e126687a9a`, verified directly:

- the commit's dtype hunk edits `DraftModelSpeculator` in the BASE
  `vllm/v1/worker/gpu/spec_decode/speculator.py`, whose `draft_logits_spec`
  returns `model_config.head_dtype, 0.0` at `:332-337`;
- **DFlash2 has its own `draft_logits_spec`, and it still returns
  `torch.float32, -float("inf")`** at
  `vllm/v1/worker/gpu/spec_decode/dflash2/speculator.py:133-137`, with a comment
  giving the reason ("fp32 so the walk and the rejection that checks it read the
  same distribution; -inf because the cache kernel writes only the K
  candidates");
- DFlash2 writes that cache with its own `_cache_draft_logits_kernel`
  (`dflash2/speculator.py:79-105`, launched at `:167`) and never through
  `gumbel_sample`'s `logits_cache`, which is the parameter the commit renames;
- the commit's file list contains `dspark/speculator.py`, `gumbel.py`,
  `rejection_sampler_utils.py` and the base `speculator.py` — **not**
  `dflash2/speculator.py`.

So neither the dtype half nor the pre-temperature half reaches DFlash2, and this
tree's record at
`include/vllm/v1/worker/gpu/spec_decode/dflash2/speculator.h:31,35-36`,
`include/vt/ops.h:1692` and `.agents/specs/dflash2-spec-decode.md:852,1627` is
**correct at the advance target**. Nothing is owed. `[80]`'s `inert` label is
unaffected and holds on its own ground — probabilistic draft sampling is refused
by name — which is why the wrong reasoning did not move a disposition.

Recorded as a retraction rather than quietly repaired, and #2668's body is
corrected in the same change, because that issue would have reached a
pin-advancing wave whether or not anyone read this file.

## 10. Carry-over

- **The pin did not advance**, and nothing here is a reason to move it. It
  remains `5559679229bc961848b121ccdeaa8fa5d79bec98`.
- The remaining **210** PORT-NOW entries stay owed by
  [#2611](https://github.com/mudler/vllm.cpp/issues/2611). Entries 1-40 are
  PORTQ-1 (#2642); 41-80 are this wave; 81-120 belong to PORTQ-3.
- Porting the nine gaps is owed by their own issues, not by this record.
- **The four deferred (`inert`) entries each have an owner and an issue**, filed
  so the note is met by the person who lands the gate rather than only by a
  reader of this file: [#2665](https://github.com/mudler/vllm.cpp/issues/2665),
  [#2666](https://github.com/mudler/vllm.cpp/issues/2666),
  [#2667](https://github.com/mudler/vllm.cpp/issues/2667),
  [#2668](https://github.com/mudler/vllm.cpp/issues/2668), whose body carried a
  false staleness claim now retracted in §9.1 and corrected on the issue itself.
  `[62]`'s
  deferred warning rides on [#2660](https://github.com/mudler/vllm.cpp/issues/2660)
  for the same reason.
- **`UPSTREAM-SYNC-HEADPIN` is still not a matrix row**, as PORTQ-1 §9 records.
  This wave's spec inherits the row line rather than introducing it, and the
  issues are carried under its `## Owed`.
- **The cheap first pass paid again, and gained a step.** Ask git and the
  committed deferral text before the upstream diff — and, new this tranche, read
  the TARGET HEAD before sizing a deletion. §6.1 is a commit whose own diff would
  have been a misleading guide to what the pin advance actually requires.
