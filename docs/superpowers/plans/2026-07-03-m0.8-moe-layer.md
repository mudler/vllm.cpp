# M0.8 MoE Layer (eager, bf16) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correctness-grade Qwen3.6 sparse-MoE block ops — router top-k, expert-loop MLP (silu-mul), weighted combine, shared expert with sigmoid gate — CPU+CUDA vs pinned-oracle goldens; plus the NVFP4(modelopt)→bf16 weight-dequant utility that M0.9 needs to materialize real weights.

**Architecture:** Same shape as M0.7: (1) semantics doc + pinned-oracle goldens first; (2) CPU refs; (3) CUDA; (4) records. Expert compute is an EXPLICIT LOOP over activated experts reusing existing `Matmul`/`SiluAndMul` where possible — new ops only where composition can't express the semantics (router top-k + normalize; scatter/combine). Fused/grouped-GEMM kernels are M2.2. The NVFP4 dequant is a host-side load-time utility (not a vt op): modelopt W4A4 format from the actual gate checkpoints.

## Global Constraints

- Pinned-checkout oracle only (layer-level rule). Sources to read FIRST and record in `.agents/specs/moe-semantics.md` with cites: `vllm/model_executor/models/qwen3_next.py::Qwen3NextSparseMoeBlock` (router type, renormalize flag, shared_expert_gate sigmoid), `vllm/model_executor/layers/fused_moe/layer.py` (+ the torch-native/reference path it exposes; find `fused_moe`'s reference implementation — check `tests/kernels/moe` utilities at the pin if the layer has no eager path — record which callable becomes the oracle), Qwen3.6 config keys (`norm_topk_prob`?), and the modelopt NVFP4 safetensors format from the 35B checkpoint's `hf_quant_config.json` + tensor naming (`*.weight`, `*.weight_scale`, `*.input_scale`? read actual tensor names from the checkpoint on dgx via our own dump_container CLI) cross-checked with `.agents/specs/gguf-nvfp4-notes.md` (UE4M3 ×0.5 trap!) and pinned `vllm/model_executor/layers/quantization/modelopt.py` dequant math.
- Golden budget ≤2MB: synthetic dims (e.g. hidden 64, 8 experts top-2, moe_intermediate 32, few tokens) + one real-RATIO case (256 experts top-8 + shared, hidden 2048 is too big → scale down but keep top-8-of-256 routing shape with hidden 128, inter 64; document).
- NVFP4 dequant verified against a REAL checkpoint tensor: dequantize one small expert weight from the 35B via our C++ utility on dgx and diff vs a python dequant using the pinned modelopt code (or the recipe from hf_quant_config.json executed in torch) — byte-level compare after bf16 round.
- New OpIds minimal: `kMoeRouterTopK` (softmax/sigmoid? per source + optional renormalize → weights+indices), `kMoeCombine` (weighted scatter-add of expert outputs + optional shared-expert term). Expert MLP itself composes existing ops in the runner/tests (and later M0.9 assembly) — no new op.
- CUDA: router + combine kernels correctness-grade; expert loop reuses cuBLASLt Matmul + CUDA SiluAndMul.
- Warnings-as-errors; TDD; dgx workflow; push authorized. Upstream-counterpart comments (`fused_moe` Triton/cutlass kernels → M2.2).

### Task 1: Semantics doc + pinned-oracle MoE goldens
`.agents/specs/moe-semantics.md` (router formula incl. softmax dtype + top-k tie behavior + renormalize; expert MLP; shared-expert gate; combine order) + `tools/parity/dump_moe.py` on dgx → goldens: `moe_router_topk_*` (small + 256e-top8 case), `moe_block_*` (full block end-to-end small case: hidden→router→experts→combine+shared — this is the layer-level golden M0.9 assembles against), committed. Also record NVFP4 tensor naming/format findings (from the real 35B via dump_container + hf_quant_config.json) in the doc — Task 4 consumes them.

### Task 2: CPU ops + parity
`kMoeRouterTopK`, `kMoeCombine` (ops.h/ops.cpp/cpu_ops.cpp), parity runners (router + full-block runner composing router→Matmul/SiluAndMul expert loop→combine), unit tests (tie-break determinism, renormalize on/off, top-8-of-256 shapes, shared-gate sigmoid). **As each runner lands, remove its op from `PendingRunnerOps()` in test_op_parity.cpp** (moe_router_topk when the router runner lands; moe_block when the block runner lands) so the goldens are actually exercised, not skipped. Verify CI green after committing (the goldens are now live).

### Task 3: CUDA ops + parity on dgx
Router (per-token block: softmax+topk over 256 — single block per token, shared-mem partial sort or full sort for correctness-grade), combine (atomic-free: per-token gather since top-k indices are per-token — thread per (token, dim) summing k expert outputs), dgx parity both passes green.

### Task 4: NVFP4→bf16 dequant utility
`src/vllm/model_executor/model_loader/nvfp4_dequant.{h,cpp}`: input = packed u8 tensor + scale tensors per modelopt format (names/layout per Task 1 findings), output bf16 buffer; unit test with synthetic block (hand-computed per the recorded format incl. the ×0.5-free correct UE4M3 semantics — modelopt safetensors scales are FP8-E4M3, NOT the GGUF fork's convention; verify from modelopt.py); dgx e2e: dequant one real 35B expert tensor, diff vs python dequant (tolerance: bf16 exact after rounding). THE GATE for M0.9 weight loading.

### Task 5: Records
roadmap M0.8 ✅; ledger; state (NEXT M0.9: assembly — gdn-semantics §6 prep + moe-semantics + nvfp4 dequant + hf_config all ready); inventory §5/§6 markers (FusedMoE row 🚧 correctness-grade; modelopt NVFP4 row 🚧 dequant-only). **Close-out assert: `PendingRunnerOps()` in test_op_parity.cpp must be empty of M0.8 ops (moe_router_topk, moe_block) — both runners landed in Tasks 2/3, so the MoE goldens are exercised on CPU+CUDA, not skipped.** CI green (verify via gh).
