# M0.9 Qwen3.6 Forward + Registry — the M0 Exit Criterion

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Assemble the Qwen3.6 model forward (embed → 40 × decoder → norm → lm_head → logits) from the vt ops built in M0.2/M0.6/M0.7/M0.8 plus a new dense causal-attention op, loading real 35B/27B weights (NVFP4 experts dequant-to-bf16 + bf16 dense), and prove **logits parity + token-for-token greedy decode** vs the pinned oracle. This closes M0.

**Architecture:** Single-sequence, non-paged, correctness-grade (no engine, no KV paging — that is M1). Mirrored placement: `src/vllm/model_executor/models/{registry,qwen3_5}.{h,cpp}` (semantics from pinned `models/qwen3_next.py`+`qwen3_5.py`), weight mapping in the loader. The forward is an explicit function over vt ops (no module system), per the design doc. The 1-in-4 full-attention layers need a new **dense causal attention** op (qk-norm + partial RoPE + GQA + output gate) — paged attention stays M1.6.

**Oracle strategy (the crux — Task 1 de-risks it):** there is NO tiny hybrid model (gate models are 27B/35B; Qwen3-0.6B is plain-dense, wrong arch). So: **per-layer goldens from real 27B weights** (one GDN decoder layer + one full-attn decoder layer + embed + final-norm + lm_head, dumped from the pinned model constructed with real weights on dgx) keep iteration cheap; **one full-model end-to-end logits+greedy comparison** on the 27B (smaller of the two gates) is the final gate. Task 1 determines exactly how to obtain both from the pinned checkout before any forward code is written.

## Global Constraints

- Pinned-checkout oracle only for all goldens (layer + full-model). The dgx oracle for constructing the real model may need more than the pip venv — Task 1 establishes the exact recipe (likely `PYTHONPATH=/home/mudler/_git/vllm` + its deps in a venv, constructing `Qwen3_5MoeForCausalLM` and running a forward with real weights; record precisely).
- Carryovers from M0.8 review (BINDING): (1) MoE assembly uses an **activated-expert token-gather loop** (not O(E)-dense); (2) post-dequant bf16 weights are `[out,in]` — transpose for Matmul's `[in,out]` B layout (or add a transposed-B matmul path); (3) the MoeCombine f32-single-round vs upstream bf16-double-round decision — MEASURE the logits impact on the full model; if greedy decode matches token-for-token, single-round is accepted and recorded; if it flips a token, implement the double-round.
- Weights: NVFP4 experts → `DequantNvfp4ToBf16` (bit-exact, M0.8); bf16 dense (attn qkv/o, gate, norms, embed, lm_head) load direct; router `gate.weight` stays bf16 (NOT dequant). Stacked-param mapping (qkv_proj, gate_up_proj) per upstream `packed_modules_mapping`. GGUF weight path (arch `qwen35moe`) is a SEPARATE task (Task 7) — safetensors first.
- Config from `hf_config` (M0.4); layer_types drives GDN-vs-full dispatch; g/beta prep (gdn-semantics §6) lands here.
- Warnings-as-errors; TDD where runnable (layer parity in CI via committed goldens; full-model run is dgx-only, nightly-style). Push authorized. dgx workflow as established.

---

### Task 1: Oracle recipe + per-layer & full-model reference dumps (27B)
Establish on dgx how to construct the pinned `Qwen3_5MoeForCausalLM` with real 27B weights and extract: (a) per-layer goldens — embed output, one GDN decoder layer (input hidden → output hidden, real dims, layer_type linear_attention), one full-attn decoder layer (layer_type full_attention, incl. qk-norm + output gate), final norm, lm_head — for a short fixed token sequence (T≈8); (b) full-model reference — final logits [T, vocab] + argmax greedy token ids for a fixed prompt. Record the exact construction recipe in `.agents/qwen36-forward-notes.md` (how the pinned model is instantiated standalone, attention-metadata stubbing for single-seq, any deps beyond the venv). Goldens under `tests/parity/goldens/qwen36_layer_*` (budget: per-layer hidden states are [T, 2048] f32 ≈ tiny; lm_head output [T, vocab=151k] f32 ≈ 4.8MB — store logits for T=2 only, or top-1000 logits + argmax; DECIDE and document to respect budget). If constructing the full pinned model standalone proves infeasible, fall back to HF transformers as the END-TO-END logits oracle (record the deviation: transformers is the model reference vLLM itself validates against) while per-layer goldens stay pinned-vLLM. **This task must produce a working oracle before any forward code — de-risks the milestone.**

### Task 2: Dense causal attention op (qk-norm + partial RoPE + GQA + output gate)
New op `kDenseAttention` (or compose): per pinned `Qwen3NextAttention` — QKV proj (caller), per-head RMSNorm on q and k, partial NeoX RoPE (reuse kRopeNeox), causal softmax(qk^T·scale) with GQA broadcast (Hq=16,Hk=2), then sigmoid output gate (attn_output_gate=True). Read pinned `models/qwen3_next.py::Qwen3NextAttention` + record formula in qwen36-forward-notes.md. CPU + CUDA, pinned goldens (synthetic small + real-dims-few-heads), parity runners. Remove from PendingRunnerOps as the runner lands (M0.8 lesson: allowlist entry co-lands with goldens if goldens precede runner).

### Task 3: Weight loader → in-memory bf16 model
`src/vllm/model_executor/models/qwen3_5_weights.{h,cpp}`: given a SafetensorsFile set (index.json → shards) + HfConfig, produce an in-memory `Qwen3_5Weights` struct (per-layer: GDN projections/conv/norms OR attn qkv/o/qk-norms/gate; MoE router gate + per-expert w13/w2 + shared expert; embed; final norm; lm_head). NVFP4 expert weights → DequantNvfp4ToBf16; bf16 dense direct; apply stacked-param mapping (qkv, gate_up) + tensor-name mapping (record the real 35B/27B naming from moe-semantics §8 + a dump_container listing). Transpose weights to Matmul's B layout at load. Unit test: load a FEW real tensors on dgx and check shapes/values vs the checkpoint; full load is exercised by Task 5.

### Task 4: Forward function + registry
`src/vllm/model_executor/models/qwen3_5.{h,cpp}`: `Qwen3_5Model::Forward(token_ids, positions) -> logits` over vt ops: embed → for each of 40 layers dispatch on layer_types (GDN path: in_proj → conv1d → g/beta prep (gdn-semantics §6) → l2norm → GdnPrefill/Decode → gated rmsnorm → out_proj; full-attn path: qkv → DenseAttention → o_proj) then MoE block (router → activated-expert gather loop → combine + shared) → residuals → final norm → lm_head. Registry: config architectures → forward. Per-layer parity in CI against Task 1 goldens (each layer type verified independently before the full stack).

### Task 5: Full-model logits parity + greedy decode (M0 EXIT GATE)
On dgx: load real 27B weights via Task 3, run Task 4 forward for the Task 1 fixed prompt, compare final logits vs the Task 1 reference (tolerance: bf16-model logits — expect looser, e.g. atol/rtol 5e-2 on logits BUT argmax/greedy token ids must match EXACTLY for ≥N generated tokens). Token-for-token greedy decode match is the real bar. If a token flips: systematic-debugging — bisect via per-layer goldens to find the divergent layer; check the MoeCombine rounding decision. Record the measured logit gap + greedy match length. Also run on the 35B if 27B passes.

### Task 6: GGUF weight path (arch qwen35moe)
Load the same model from the APEX GGUF (or a real NVFP4 GGUF if generated): GGUF tensor-name → param mapping (qwen35moe arch), k-quant/NVFP4-GGUF dequant to bf16 (the GGUF fork NVFP4 id 40 path + ×0.5 UE4M3 trap from gguf-nvfp4-notes.md — DISTINCT from modelopt), verify greedy decode matches the safetensors path (or the reference). This satisfies the GGUF gate at model level.

### Task 7: Records → M0 EXIT
roadmap M0.9 ✅ + **M0 exit criterion met**; ledger (logit gap + greedy match); state (M0 DONE — model produces correct tokens; NEXT M1.1 engine core/Request/SamplingParams); inventory §5 Qwen3.5 row → ✅ correctness-grade; README model table → Qwen3.6 row "🚧 forward parity (M0.9); serving M1-M3"; close-out asserts PendingRunnerOps empty + CI green.

## Self-review notes
- Task 1 de-risks the oracle (the milestone's biggest unknown) before code.
- Per-layer goldens keep CI cheap; the expensive full-model run is dgx-only and gated on cheap layer parity first (systematic-debugging bisect path built in).
- All M0.8 carryovers (expert-gather loop, weight transpose, combine-rounding) are explicit tasks/constraints.
- Full attention scoping question RESOLVED: Task 2 delivers a dense correctness-grade op; paged is M1.6.
