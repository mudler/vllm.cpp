# M1.2 BlockPool + Prefix Caching

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** The physical KV block store — `KVCacheBlock`, `BlockPool` (free list + ref counts + `BlockHash → block` map for prefix reuse + LRU eviction), and the block-hash machinery (parent-chained hashes over block-sized token spans) — ported 1:1 from vLLM V1 so M1.3 (KVCacheManager) can allocate/reuse/free blocks. Behavioral (no model, no CUDA); unit tests ported from upstream `tests/v1/core/`.

**Architecture:** Mirrored placement `src/vllm/v1/core/block_pool.{h,cpp}` (← `vllm/v1/core/block_pool.py`), `src/vllm/v1/core/kv_cache_utils.{h,cpp}` (← `vllm/v1/core/kv_cache_utils.py` — the hashing + `KVCacheBlock`/`FreeKVCacheBlockQueue`/`BlockHash`/`hash_block_tokens`/`hash_request_tokens`). This is the prefix-caching heart: identical token+group prefixes share physical blocks. Data structures + logic only; the manager that decides *when* to allocate is M1.3.

**Tech Stack:** C++20, doctest. No CUDA/model. All CI. Hashing: port upstream's default (it uses a builtin hash / sha256 / xxhash by config — for T0 port the DEFAULT constructor-hash path; the exact algo must match upstream so a ported test's expected hashes line up — READ which hash upstream uses by default and replicate, OR make the hash pluggable and test with a deterministic stub, matching how upstream's tests inject hashes).

## Global Constraints

- 1:1 with upstream V1: same class names (`KVCacheBlock`, `FreeKVCacheBlockQueue`, `BlockPool`, `BlockHash`, `BlockHashWithGroupId`), same method names (`get_new_blocks`, `cache_full_blocks`, `get_cached_block`, `touch`, `free_blocks`, `_maybe_evict_cached_block`, `reset_prefix_cache`, `get_num_free_blocks`), same field names, same semantics. Ported files carry `// Ported from: vllm/v1/core/<file> @ e24d1b24`. Read upstream BEFORE writing.
- The free-list is upstream's intrusive doubly-linked LRU (`FreeKVCacheBlockQueue` with `prev_free_block`/`next_free_block` on `KVCacheBlock`) — port that exact structure (eviction order + O(1) remove depend on it), not a std::list swap.
- ref_cnt semantics + the "cached but unreferenced blocks are evictable in LRU order" invariant must match upstream exactly (this is the correctness core of prefix caching).
- Port upstream's OWN unit tests from `/home/mudler/_git/vllm/tests/v1/core/test_prefix_caching.py` + `test_kv_cache_utils.py` (the BlockPool/hash-relevant cases) — cite which cases; these are the behavioral oracle (no golden dumps).
- Warnings-as-errors; TDD; push authorized (CPU, all CI).

---

### Task 1: KVCacheBlock + FreeKVCacheBlockQueue (intrusive LRU free list)
Read `/home/mudler/_git/vllm/vllm/v1/core/kv_cache_utils.py` (`KVCacheBlock`, `FreeKVCacheBlockQueue`). Port to `kv_cache_utils.{h,cpp}`: `KVCacheBlock` (block_id, ref_cnt, block_hash optional, prev/next_free_block links, `incr_ref`/`decr_ref`, `reset_hash`), `FreeKVCacheBlockQueue` (intrusive doubly-linked list with fake head/tail sentinels: `popleft`, `remove`, `append`, `get_all_free_blocks`, `num_free_blocks`, `popleft_n`?) — port the EXACT pointer manipulation (upstream relies on it for O(1) LRU remove of an arbitrary cached block). Unit tests ported from upstream test_kv_cache_utils.py free-queue cases (popleft order = FIFO/LRU, remove-from-middle, append-to-tail, num_free_blocks accounting).

### Task 2: Block hashing (parent-chained, group-aware)
Read the hashing in `kv_cache_utils.py`: `BlockHash`, `BlockHashWithGroupId`, `hash_block_tokens`, `hash_request_tokens`, `NONE_HASH`, the parent-hash chaining (each block's hash includes the previous block's hash + this block's token ids + optional extra keys). Port to `kv_cache_utils.{h,cpp}`. Match upstream's default hash function so ported test vectors line up (if upstream default is python `hash()` seeded / sha256 / a specific constant NONE_HASH — replicate the DEFAULT the tests use; if the tests inject a deterministic hash fn, do the same via a pluggable hasher). Unit tests ported from test_kv_cache_utils.py hashing cases (hash_request_tokens produces N block hashes for N full blocks, parent chaining changes the hash, group id differentiates, partial last block not hashed).

### Task 3: BlockPool (allocate / cache / reuse / evict / free)
Read `/home/mudler/_git/vllm/vllm/v1/core/block_pool.py` fully. Port `block_pool.{h,cpp}`: `BlockPool(num_gpu_blocks, enable_caching)` with the block array + `FreeKVCacheBlockQueue` + `cached_block_hash_to_block` map (`BlockHashWithGroupId → {block_id → KVCacheBlock}`); methods `get_new_blocks(n)` (pop from free queue, evict-cached-on-reuse via `_maybe_evict_cached_block`), `cache_full_blocks(...)` (insert fully-computed blocks into the hash map for reuse — the full signature: request, blocks, block_hashes, num_cached/full, block_size, hash_fn), `get_cached_block(block_hash, group_ids)` (lookup for prefix hit), `touch(blocks)` (ref-count + remove from free queue), `free_blocks(ordered_blocks)` (decr ref, append to free queue when ref_cnt hits 0), `_maybe_evict_cached_block`, `reset_prefix_cache`, `get_num_free_blocks`, `get_usage`, the null-block handling (block 0). Defer KV-cache-events (`take_events`) + the metrics to marked stubs. Unit tests ported from upstream test_prefix_caching.py BlockPool-level cases (allocate reduces free count, cache_full_blocks makes a block reusable, get_cached_block returns it, touch prevents eviction, free returns to pool, eviction reuses LRU cached block, reset_prefix_cache clears).

### Task 4: Records
roadmap M1.2 ✅; ledger row (BlockPool + prefix-cache hashing ported 1:1, upstream tests ported); state (NEXT M1.3 KVCacheManager + hybrid coordinator — the gate models need the GDN-state group alongside the full-attn block group); inventory §2 mark BlockPool/prefix-caching rows ✅; CI green. No goldens.

## Self-review notes
- The intrusive LRU free queue + ref_cnt + cached-hash-map are the correctness core — port the exact pointer/refcount logic, validated by upstream's own ported tests (the behavioral oracle).
- Hashing must match upstream's default so ported test expected-hashes line up; if that's impractical, use the pluggable-hasher approach upstream's tests themselves use.
- No CUDA/model — this is pure engine data-structure logic, fully CI-testable, the foundation M1.3's KVCacheManager builds on.
