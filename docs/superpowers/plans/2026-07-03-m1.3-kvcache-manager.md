# M1.3 KVCacheManager + Hybrid Coordinator

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** The KV cache allocation brain — `KVCacheSpec` hierarchy (`FullAttentionSpec`, `MambaSpec` for GDN state), `KVCacheConfig`/`KVCacheGroupSpec`, the per-group `SingleTypeKVCacheManager`s, the `KVCacheCoordinator` (hybrid: full-attn block group + GDN-state group), and `KVCacheManager` (`get_computed_blocks` for prefix reuse, `allocate_slots`, `free`) — ported 1:1 from vLLM V1 on top of M1.2's BlockPool. This is what the scheduler (M1.4) calls to admit/preempt requests. Behavioral (no model/CUDA); tests ported from upstream `tests/v1/core/`.

**Architecture:** Mirrored: `src/vllm/v1/kv_cache_interface.{h,cpp}` (← `vllm/v1/kv_cache_interface.py` — the specs), `src/vllm/v1/core/single_type_kv_cache_manager.{h,cpp}`, `src/vllm/v1/core/kv_cache_coordinator.{h,cpp}`, `src/vllm/v1/core/kv_cache_manager.{h,cpp}`. The hybrid coordinator is T0 because the gate models interleave GDN (mamba-state) and full-attention layers — two KV cache groups with different specs. Scope T0: FullAttentionSpec + MambaSpec + the unified/hybrid coordinator path the gate models need; defer SlidingWindow/MLA/cross/encoder specs (T1/T2) behind marked stubs.

**Tech Stack:** C++20, doctest. No CUDA/model. All CI. Builds on M1.2 (BlockPool, KVCacheBlock, Request.block_hashes).

## Global Constraints

- 1:1 with pinned e24d1b24 — VERIFY the pinned file's CURRENT API before porting each class (M1.2 lesson: don't assume an older shape). Same class/method/field names. Ported files carry `// Ported from: vllm/v1/<path> @ e24d1b24`.
- `KVCacheBlocks` (the grouped-block wrapper returned by allocate/get_computed) + the exact `allocate_slots` block-accounting (comp | new_comp | new | lookahead layout) must match upstream — this is where preemption/prefix-cache correctness lives.
- Hybrid coordinator: the gate models = full-attn group (paged blocks) + GDN group (mamba state, `MambaSpec`). Port `get_kv_cache_coordinator` / the `HybridKVCacheCoordinator` vs `UnitaryKVCacheCoordinator` selection + `find_longest_cache_hit` across groups. Defer the sliding-window/chunked-local specifics (T1).
- Port upstream's OWN tests from `/home/mudler/_git/vllm/tests/v1/core/test_kv_cache_manager.py` (+ test_prefix_caching.py manager-level cases, test_kv_cache_utils.py spec cases). Cite. Assert the literal `test_evict` manager-level free order `[6,10,5,4,3,2,1]` (M1.2 carry-forward).
- Warnings-as-errors; TDD; push authorized (CPU, all CI).

---

### Task 1: KVCacheSpec hierarchy + KVCacheConfig
Read `/home/mudler/_git/vllm/vllm/v1/kv_cache_interface.py`. Port the T0 specs: `KVCacheSpec` (base: block_size, page_size_bytes, type_id), `AttentionSpec` (num_kv_heads, head_size, dtype), `FullAttentionSpec`, `MambaSpec` (the GDN/mamba state spec — shapes/dtypes, page_size_bytes for state), and the config wrappers `KVCacheTensor`, `KVCacheGroupSpec`, `KVCacheConfig`. Match field names + the `page_size_bytes`/`max_memory_usage_bytes` computations. Defer SlidingWindowSpec/MLAAttentionSpec/ChunkedLocalAttentionSpec/Cross/EncoderOnly behind marked stubs (T1/T2). Unit tests ported from test_kv_cache_utils.py spec cases (page_size_bytes for full-attn + mamba, group spec construction).

### Task 2: SingleTypeKVCacheManager (per-group)
Read `/home/mudler/_git/vllm/vllm/v1/core/single_type_kv_cache_manager.py`. Port `SingleTypeKVCacheManager` base + `FullAttentionManager` + `MambaManager` (the T0 pair for the gate models). Methods: `get_num_blocks_to_allocate`, `save_new_computed_blocks`, `allocate_new_blocks`, `cache_blocks`, `get_num_common_prefix_blocks`, `remove_skipped_blocks`, `find_longest_cache_hit` (the per-type prefix-match), `free`. Match the block-table bookkeeping per request. Defer sliding-window/chunked managers (T1). Unit tests ported from upstream (allocate/cache/free per group, prefix hit within a group, mamba manager's single-state-block behavior).

### Task 3: KVCacheCoordinator (hybrid)
Read `/home/mudler/_git/vllm/vllm/v1/core/kv_cache_coordinator.py`. Port the coordinator: `KVCacheCoordinator` base, `UnitaryKVCacheCoordinator` (single group), `HybridKVCacheCoordinator` (multi-group — the gate models), and `get_kv_cache_coordinator(...)` selection. Methods: `get_num_blocks_to_allocate`, `allocate_new_blocks` (across groups), `cache_blocks`, `find_longest_cache_hit` (cross-group prefix match — the subtle part: the shared prefix must be cached in ALL groups), `free`, `get_num_common_prefix_blocks`. This is where hybrid correctness lives. Unit tests ported from upstream coordinator tests (hybrid allocate across 2 groups, cross-group prefix hit, the GDN+full-attn shape).

### Task 4: KVCacheManager (top-level)
Read `/home/mudler/_git/vllm/vllm/v1/core/kv_cache_manager.py`. Port `KVCacheManager` + `KVCacheBlocks`: `get_computed_blocks(request)` → (KVCacheBlocks, num_computed_tokens) using the coordinator's prefix match; `allocate_slots(request, num_new_tokens, num_lookahead_tokens, ...)` — the central allocator (the comp|new_comp|new|lookahead block layout; returns None when OOM → scheduler preempts); `free(request)`; `get_block_ids(request)`; `reset_prefix_cache`; `get_num_common_prefix_blocks`. Match upstream's exact allocation accounting + the None-on-OOM contract. Unit tests ported from test_kv_cache_manager.py (get_computed_blocks prefix hit, allocate_slots success + OOM→None, free returns blocks, the literal test_evict order, prefix cache across requests, hybrid GDN+full-attn allocation).

### Task 5: Records
roadmap M1.3 ✅; ledger row; state (NEXT M1.4 Scheduler — the unified token-budget scheduling with chunked prefill + preemption, calling this KVCacheManager); inventory §2 mark KVCacheManager/coordinator/specs ✅ (note deferred sliding-window/MLA); CI green. No goldens.

## Self-review notes
- The hybrid coordinator + allocate_slots accounting are the correctness core — port exactly, validate with upstream's ported tests.
- Verify the pinned API per class before porting (M1.2 lesson).
- T0 = FullAttention + Mamba(GDN) + hybrid coordinator (what the gate models need); sliding-window/MLA/etc deferred behind 1:1 stubs that throw if hit.
- No CUDA/model — pure allocation logic, CI-testable, the layer M1.4's scheduler drives.
