// GENERATED by scripts/gen-ltx2-dfr-goldens.py — DO NOT EDIT BY HAND.
//
// LTX-2.5 DFR CANVAS LAYOUT goldens (row LTX25-DFR-PIPELINE, issue #986),
// produced by IMPORTING AND EXECUTING upstream's `ltx_pipelines.dfr_layout`
// and the three pure-index helpers of `ltx_pipelines.dfr_pipeline`.
//
// Every value here is an INDEX. A port that gets one wrong still produces a
// correctly shaped, finite, plausible latent, so these exact vectors are the
// only instrument that can see the defect — which is why the suite compares
// them element-wise rather than checking a property.
//
// Upstream revision (Lightricks/LTX-2): fd4ded7f2d88d3da713abcdd4ad41ecc4a9314ca
#pragma once

#include <cstdint>

namespace vllm_test {

// The upstream tree these numbers came from. The suite asserts this equals the
// SHA it pins, so regenerating against a DIFFERENT checkout fails the gate
// instead of silently replacing the oracle. The generator REFUSES a dirty
// tree, so this SHA always describes the code that ran.
inline constexpr const char* kLtx2DfrUpstreamRevision = "fd4ded7f2d88d3da713abcdd4ad41ecc4a9314ca";

// VIDEO_SCALE_FACTORS.time, read off the executed module rather than restated.
inline constexpr int64_t kLtx2DfrTemporalScale = 8;

// --------------------------------------------------------------------------
// 1. SEGMENT_CANDIDATES and choose_segment_length (dfr_layout.py:12, :40-57)
// --------------------------------------------------------------------------
//
// The candidates are read off the module's own tuple, so a candidate added
// upstream changes this gate rather than sliding past it.
inline constexpr int64_t kLtx2DfrSegmentCandidatesGolden[] = {24, 32};
inline constexpr int64_t kLtx2DfrSegmentCandidatesGoldenCount = 2;

inline constexpr int64_t kLtx2DfrTileLeadSegmentsGolden = 1;

// `content_frames` inputs and the segment upstream chose for each. The set is
// built to contain a TIE (both pads equal), an exact multiple of each
// candidate, and counts where each candidate wins outright — so a port that
// resolved the tie the other way, or that seeded its running best with the
// largest candidate, is separated from a correct one by this table alone.
inline constexpr int64_t kLtx2DfrSegmentContent[] = {8, 24, 32, 48, 64, 72, 96, 120, 128, 144, 168, 192, 200, 240, 264, 288};
inline constexpr int64_t kLtx2DfrSegmentContentCount = 16;

inline constexpr int64_t kLtx2DfrSegmentChosen[] = {24, 24, 32, 24, 32, 24, 32, 24, 32, 24, 24, 32, 24, 24, 24, 32};
inline constexpr int64_t kLtx2DfrSegmentChosenCount = 16;

// The pad each candidate would take, in SEGMENT_CANDIDATES order. Emitted so
// the suite can assert that a tie is PRESENT in the table rather than trusting
// that one was included — an intent that no longer holds is the failure this
// campaign has already paid for.
inline constexpr int64_t kLtx2DfrSegmentPads[] = {16, 24, 0, 8, 16, 0, 0, 16, 8, 0, 0, 24, 0, 0, 0, 8, 16, 0, 0, 16, 0, 24, 0, 0, 16, 24, 0, 16, 0, 24, 0, 0};
inline constexpr int64_t kLtx2DfrSegmentPadsCount = 32;

inline constexpr int64_t kLtx2DfrSegmentCandidateCount = 2;

// --------------------------------------------------------------------------
// 2. resolve_canvas (dfr_layout.py:60-81)
// --------------------------------------------------------------------------
// Requested frame counts, all satisfying `(n - 1) % 8 == 0`, and what
// `resolve_canvas` returned for each: the PADDED canvas, the segment, and the
// keyframe positions flattened with a per-case count.
inline constexpr int64_t kLtx2DfrCanvasRequest[] = {9, 25, 33, 49, 73, 97, 121, 129, 193, 201, 241, 265};
inline constexpr int64_t kLtx2DfrCanvasRequestCount = 12;

inline constexpr int64_t kLtx2DfrCanvasFrames[] = {25, 25, 33, 49, 73, 97, 121, 129, 193, 217, 241, 265};
inline constexpr int64_t kLtx2DfrCanvasFramesCount = 12;

inline constexpr int64_t kLtx2DfrCanvasSegment[] = {24, 24, 32, 24, 24, 32, 24, 32, 32, 24, 24, 24};
inline constexpr int64_t kLtx2DfrCanvasSegmentCount = 12;

inline constexpr int64_t kLtx2DfrCanvasPositionCount[] = {1, 1, 1, 2, 3, 3, 5, 4, 6, 9, 10, 11};
inline constexpr int64_t kLtx2DfrCanvasPositionCountCount = 12;

inline constexpr int64_t kLtx2DfrCanvasPositions[] = {24, 24, 32, 24, 48, 24, 48, 72, 32, 64, 96, 24, 48, 72, 96, 120, 32, 64, 96, 128, 32, 64, 96, 128, 160, 192, 24, 48, 72, 96, 120, 144, 168, 192, 216, 24, 48, 72, 96, 120, 144, 168, 192, 216, 240, 24, 48, 72, 96, 120, 144, 168, 192, 216, 240, 264};
inline constexpr int64_t kLtx2DfrCanvasPositionsCount = 56;


// --------------------------------------------------------------------------
// 3. tile_ranges (dfr_layout.py:137-182)
// --------------------------------------------------------------------------
//
// One flattened record per tile, in tile order, with a per-case tile count.
// The two keyframe bags are flattened separately with their own counts,
// because their lengths differ per tile and a single interleaved stream would
// make an off-by-one in one bag readable as a correct value in the other.
inline constexpr int64_t kLtx2DfrTileCaseRequest[] = {97, 97, 121, 121, 193, 193, 241, 241};
inline constexpr int64_t kLtx2DfrTileCaseRequestCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseRounds[] = {1, 2, 1, 2, 1, 2, 1, 2};
inline constexpr int64_t kLtx2DfrTileCaseRoundsCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseFrames[] = {193, 193, 241, 241, 385, 385, 481, 481};
inline constexpr int64_t kLtx2DfrTileCaseFramesCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseNumTiles[] = {2, 4, 2, 4, 2, 4, 2, 4};
inline constexpr int64_t kLtx2DfrTileCaseNumTilesCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseTileCount[] = {2, 3, 2, 4, 2, 4, 2, 4};
inline constexpr int64_t kLtx2DfrTileCaseTileCountCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseSeamCount[] = {3, 3, 5, 5, 6, 6, 10, 10};
inline constexpr int64_t kLtx2DfrTileCaseSeamCountCount = 8;

inline constexpr int64_t kLtx2DfrTileCaseSeams[] = {64, 128, 192, 64, 128, 192, 48, 96, 144, 192, 240, 48, 96, 144, 192, 240, 64, 128, 192, 256, 320, 384, 64, 128, 192, 256, 320, 384, 48, 96, 144, 192, 240, 288, 336, 384, 432, 480, 48, 96, 144, 192, 240, 288, 336, 384, 432, 480};
inline constexpr int64_t kLtx2DfrTileCaseSeamsCount = 48;

inline constexpr int64_t kLtx2DfrTilePixelStart[] = {0, 64, 0, 0, 64, 0, 96, 0, 48, 96, 144, 0, 128, 0, 64, 192, 256, 0, 192, 0, 96, 240, 336};
inline constexpr int64_t kLtx2DfrTilePixelStartCount = 23;

inline constexpr int64_t kLtx2DfrTilePixelEnd[] = {128, 192, 64, 128, 192, 144, 240, 96, 144, 192, 240, 192, 384, 128, 256, 320, 384, 240, 480, 144, 288, 384, 480};
inline constexpr int64_t kLtx2DfrTilePixelEndCount = 23;

inline constexpr int64_t kLtx2DfrTileLatentStart[] = {0, 8, 0, 0, 8, 0, 12, 0, 6, 12, 18, 0, 16, 0, 8, 24, 32, 0, 24, 0, 12, 30, 42};
inline constexpr int64_t kLtx2DfrTileLatentStartCount = 23;

inline constexpr int64_t kLtx2DfrTileLatentEndExclusive[] = {17, 25, 9, 17, 25, 19, 31, 13, 19, 25, 31, 25, 49, 17, 33, 41, 49, 31, 61, 19, 37, 49, 61};
inline constexpr int64_t kLtx2DfrTileLatentEndExclusiveCount = 23;

inline constexpr int64_t kLtx2DfrTileDropLatentPrefix[] = {0, 9, 0, 9, 9, 0, 7, 0, 7, 7, 7, 0, 9, 0, 9, 9, 9, 0, 7, 0, 7, 7, 7};
inline constexpr int64_t kLtx2DfrTileDropLatentPrefixCount = 23;

inline constexpr int64_t kLtx2DfrTileAnchorCount[] = {2, 3, 1, 2, 3, 3, 4, 2, 3, 3, 3, 3, 5, 2, 4, 3, 3, 5, 7, 3, 5, 4, 4};
inline constexpr int64_t kLtx2DfrTileAnchorCountCount = 23;

inline constexpr int64_t kLtx2DfrTileAnchors[] = {64, 128, 64, 128, 192, 64, 64, 128, 64, 128, 192, 48, 96, 144, 96, 144, 192, 240, 48, 96, 48, 96, 144, 96, 144, 192, 144, 192, 240, 64, 128, 192, 128, 192, 256, 320, 384, 64, 128, 64, 128, 192, 256, 192, 256, 320, 256, 320, 384, 48, 96, 144, 192, 240, 192, 240, 288, 336, 384, 432, 480, 48, 96, 144, 96, 144, 192, 240, 288, 240, 288, 336, 384, 336, 384, 432, 480};
inline constexpr int64_t kLtx2DfrTileAnchorsCount = 77;

inline constexpr int64_t kLtx2DfrTileSlotCount[] = {2, 2, 1, 2, 2, 3, 3, 2, 2, 2, 2, 3, 4, 2, 3, 2, 2, 5, 6, 3, 4, 3, 3};
inline constexpr int64_t kLtx2DfrTileSlotCountCount = 23;

inline constexpr int64_t kLtx2DfrTileSlots[] = {32, 96, 96, 160, 32, 32, 96, 96, 160, 24, 72, 120, 120, 168, 216, 24, 72, 72, 120, 120, 168, 168, 216, 32, 96, 160, 160, 224, 288, 352, 32, 96, 96, 160, 224, 224, 288, 288, 352, 24, 72, 120, 168, 216, 216, 264, 312, 360, 408, 456, 24, 72, 120, 120, 168, 216, 264, 264, 312, 360, 360, 408, 456};
inline constexpr int64_t kLtx2DfrTileSlotsCount = 63;


// --------------------------------------------------------------------------
// 4. stitch_tile_latents (dfr_layout.py:185-208)
// --------------------------------------------------------------------------
//
// The stitch is gated on IDENTITY rather than on a value: each tile latent is
// filled with its own GLOBAL latent index, so the stitched result reads back
// as the exact latent frames the canvas should contain. A misplaced tile, a
// dropped prefix of the wrong size or a reversed concatenation all move a
// value that the suite can name, where a random fill would only move a digest.
inline constexpr int64_t kLtx2DfrStitchRequest = 121;
inline constexpr int64_t kLtx2DfrStitchFrames = 241;
inline constexpr int64_t kLtx2DfrStitchChannels = 2;
inline constexpr int64_t kLtx2DfrStitchHeight = 1;
inline constexpr int64_t kLtx2DfrStitchWidth = 2;
inline constexpr int64_t kLtx2DfrStitchOutFrames = 31;
// The stitched volume, [B, C, T, H, W] row-major and flattened in that order.
inline constexpr int64_t kLtx2DfrStitchExpected[] = {0, 1, 1000, 1001, 2000, 2001, 3000, 3001, 4000, 4001, 5000, 5001, 6000, 6001, 7000, 7001, 8000, 8001, 9000, 9001, 10000, 10001, 11000, 11001, 12000, 12001, 13000, 13001, 14000, 14001, 15000, 15001, 16000, 16001, 17000, 17001, 18000, 18001, 19000, 19001, 20000, 20001, 21000, 21001, 22000, 22001, 23000, 23001, 24000, 24001, 25000, 25001, 26000, 26001, 27000, 27001, 28000, 28001, 29000, 29001, 30000, 30001, 10, 11, 1010, 1011, 2010, 2011, 3010, 3011, 4010, 4011, 5010, 5011, 6010, 6011, 7010, 7011, 8010, 8011, 9010, 9011, 10010, 10011, 11010, 11011, 12010, 12011, 13010, 13011, 14010, 14011, 15010, 15011, 16010, 16011, 17010, 17011, 18010, 18011, 19010, 19011, 20010, 20011, 21010, 21011, 22010, 22011, 23010, 23011, 24010, 24011, 25010, 25011, 26010, 26011, 27010, 27011, 28010, 28011, 29010, 29011, 30010, 30011};
inline constexpr int64_t kLtx2DfrStitchExpectedCount = 124;


// --------------------------------------------------------------------------
// 5. _slot_initials_from_video (dfr_pipeline.py:101-111)
// --------------------------------------------------------------------------
//
// Same identity fill: the seed volume reads back as the latent index each slot
// selected, so the assertion names the FRAME rather than a digest of it.
//
// The positions include 52, 60 and 68, whose quotients are 6.5, 7.5 and 8.5.
// Those three SEPARATE the three plausible ports: Python's `round` is
// half-to-EVEN and gives 6, 8, 8; C's `std::round` is half-away-from-zero and
// gives 7, 8, 9; a floor gives 6, 7, 8. No two agree on all three, so the
// golden names the rounding mode rather than merely being consistent with it.
// No canvas this engine can build reaches a half quotient — every slot position
// is an odd multiple of a segment length and both candidates divide by 8 — so
// this is the only place the distinction is observable at all.
inline constexpr int64_t kLtx2DfrSeedFrames = 12;
inline constexpr int64_t kLtx2DfrSeedPositions[] = {0, 8, 24, 40, 52, 60, 68, 95, 200};
inline constexpr int64_t kLtx2DfrSeedPositionsCount = 9;

// The latent frame index each position selected, INCLUDING the clamp: 200/8 is
// 25, past the 12-frame window, and upstream clamps to T-1 rather than raising.
inline constexpr int64_t kLtx2DfrSeedSelected[] = {0, 1, 3, 5, 6, 8, 8, 11, 11};
inline constexpr int64_t kLtx2DfrSeedSelectedCount = 9;


// --------------------------------------------------------------------------
// 6. _merge_carry_forward_keyframes (dfr_pipeline.py:114-139)
// --------------------------------------------------------------------------
//
// The case DELIBERATELY overlaps a slot onto an anchor position, because that
// overlap is the only thing that distinguishes upstream's insertion order from
// its reverse. Every shape and every count is identical either way; only the
// CONTENT at the shared position moves, and it moves to a value that is still
// a valid keyframe latent.
inline constexpr int64_t kLtx2DfrMergeAnchorPositions[] = {24, 48, 72};
inline constexpr int64_t kLtx2DfrMergeAnchorPositionsCount = 3;

inline constexpr int64_t kLtx2DfrMergeSlotPositions[] = {12, 48, 60};
inline constexpr int64_t kLtx2DfrMergeSlotPositionsCount = 3;

// Anchors carry `1000 + position`, slots carry `2000 + position`, so the tag at
// the shared position 48 says which side won.
inline constexpr int64_t kLtx2DfrMergePositions[] = {12, 24, 48, 60, 72};
inline constexpr int64_t kLtx2DfrMergePositionsCount = 5;

inline constexpr int64_t kLtx2DfrMergeValues[] = {2012, 1024, 2048, 2060, 1072};
inline constexpr int64_t kLtx2DfrMergeValuesCount = 5;


// --------------------------------------------------------------------------
// 7. The pipeline constants (dfr_pipeline.py:72-78, :534)
// --------------------------------------------------------------------------
//
// Read off the executed module's own module-level names, not retyped. The
// ancestral eta in particular is NOT the distilled sampler's 1.0, and a port
// that reused that constant would inject twice the noise into a refinement
// that cannot remove it.
inline constexpr double kLtx2DfrAnchorStrengthGolden = 0.95;
inline constexpr double kLtx2DfrTemporalEtaGolden = 0.5;
inline constexpr double kLtx2DfrMaxConditioningFpsGolden = 60.0;
// `(requested - 1) * 2**rounds + 1` (dfr_pipeline.py:534), evaluated upstream's
// way for each (requested, rounds) pair.
inline constexpr int64_t kLtx2DfrTrimRequested[] = {97, 97, 97, 121, 121, 121, 193, 193, 193, 241, 241, 241};
inline constexpr int64_t kLtx2DfrTrimRequestedCount = 12;

inline constexpr int64_t kLtx2DfrTrimRounds[] = {0, 1, 2, 0, 1, 2, 0, 1, 2, 0, 1, 2};
inline constexpr int64_t kLtx2DfrTrimRoundsCount = 12;

inline constexpr int64_t kLtx2DfrTrimTarget[] = {97, 193, 385, 121, 241, 481, 193, 385, 769, 241, 481, 961};
inline constexpr int64_t kLtx2DfrTrimTargetCount = 12;


}  // namespace vllm_test
