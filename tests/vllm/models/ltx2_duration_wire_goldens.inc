// clang-format off
#pragma once
#include <cstdint>

namespace vllm_test {

// GENERATED by scripts/gen-ltx2-duration-wire-goldens.py. DO NOT EDIT.
// Upstream: Lightricks/LTX-2 @ fd4ded7f, executed -- not transcribed.
// Row LTX25-DURATION-HEAD-WIRE, issue #2900.

// snap_frames_to_grid (utils/helpers.py:554-562), time_scale = 8.
// `rejected` is the CEILING to the same grid: it agrees with upstream on
// every input that is already a grid point, which is why the emitter
// refuses a case table where only such points appear.
struct Ltx2SnapCase { int64_t frames; int64_t expected; int64_t rejected_ceil; };
inline constexpr Ltx2SnapCase kLtx2SnapCases[] = {
    {1, 1, 1},
    {2, 1, 9},
    {7, 1, 9},
    {8, 1, 9},
    {9, 9, 9},
    {10, 9, 17},
    {16, 9, 17},
    {17, 17, 17},
    {24, 17, 25},
    {25, 25, 25},
    {33, 33, 33},
    {64, 57, 65},
    {65, 65, 65},
    {121, 121, 121},
    {200, 193, 201},
    {201, 201, 201},
    {1024, 1017, 1025},
};
inline constexpr int kLtx2SnapSeparatingCases = 9;

// seconds_to_clamped_num_frames (utils/helpers.py:565-585).
// Three rejected rules ride beside each answer: clamping AFTER the snap,
// omitting the undershoot repair, and truncating where upstream rounds.
// A case where all four agree proves nothing, so the counters below say
// how many cases actually separate each pair and the test asserts they
// are non-zero -- a fixture that stopped discriminating fails loudly
// instead of passing quietly.
struct Ltx2ClampCase {
    double seconds; double frame_rate; int64_t min_frames; int64_t max_frames;
    int64_t expected; int64_t rejected_snap_first; int64_t rejected_no_repair;
    int64_t rejected_truncate; int64_t rejected_half_away;
};
inline constexpr Ltx2ClampCase kLtx2ClampCases[] = {
    {3.0, 24.0, 24, 480, 65, 65, 65, 65, 65},
    {0.1, 24.0, 24, 480, 25, 24, 17, 25, 25},
    {99.0, 24.0, 24, 480, 473, 480, 473, 473, 473},
    {1.0, 24.0, 24, 480, 25, 24, 17, 25, 25},
    {20.0, 24.0, 24, 480, 473, 473, 473, 473, 473},
    {0.5, 30.0, 5, 480, 9, 9, 9, 9, 9},
    {0.01, 25.0, 3, 9, 9, 3, 1, 9, 9},
    {0.01, 25.0, 5, 5, 5, 5, 1, 5, 5},
    {2.5, 24.0, 1, 1024, 57, 57, 57, 57, 57},
    {1.9583333, 24.0, 1, 1024, 41, 41, 41, 41, 41},
    {0.9999, 25.0, 1, 1024, 25, 25, 25, 17, 25},
    {7.0, 30.0, 30, 240, 209, 209, 209, 209, 209},
    {13.7, 24.0, 24, 240, 233, 240, 233, 233, 233},
    {4.0, 8.0, 8, 64, 25, 25, 25, 25, 25},
    {0.34, 25.0, 1, 1024, 1, 1, 1, 1, 9},
    {0.5, 49.0, 1, 1024, 17, 17, 17, 17, 25},
    {0.5, 25.0, 1, 1024, 9, 9, 9, 9, 9},
    {1.7, 25.0, 1, 1024, 41, 41, 41, 41, 41},
};
inline constexpr int kLtx2ClampSeparatingSnapFirst = 5;
inline constexpr int kLtx2ClampSeparatingNoRepair = 4;
inline constexpr int kLtx2ClampSeparatingTruncate = 1;
inline constexpr int kLtx2ClampSeparatingHalfAway = 2;

// DurationPredictor.__call__ (utils/blocks.py:850-889) end to end, on a
// head whose weights are a closed form of the parameter index. The C++
// side builds the SAME bag from the SAME expression, so a disagreement
// here is the driver and not the weights.
//
// `seconds` is the head's raw prediction, carried so a frame-count
// mismatch localizes to the forward or to the snapping rather than
// arriving as one wrong integer.
struct Ltx2PredictCase {
    int64_t video_tokens; int64_t audio_tokens; double frame_rate;
    double min_seconds; double max_seconds; float seconds; int64_t frames;
};
inline constexpr Ltx2PredictCase kLtx2PredictCases[] = {
    {7, 5, 24.0, 1.0, 20.0, 7.17012787f, 169},
    {7, 0, 24.0, 1.0, 20.0, 9.34426594f, 217},
    {0, 5, 24.0, 1.0, 20.0, 7.17013884f, 169},
    {13, 11, 30.0, 1.0, 20.0, 7.16665936f, 209},
    {7, 5, 25.0, 2.0, 4.0, 7.17012787f, 97},
};
inline constexpr int kLtx2PredictDistinctFrameCounts = 4;

// require_num_frames_source (utils/blocks.py:894-905). The MESSAGE is
// pinned so the port's refusal says what upstream's says; the POSITION
// -- top of __call__, before any work -- is what the ordering case in
// test_ltx2_video gates, and it is the half a message check cannot see.
inline constexpr const char* kLtx2RequireNumFramesMessage =
    "num_frames was AutoDuration but this checkpoint has no DurationHead weights to auto-predict duration from (DurationHead ships from LTX 2.5 / gemma4 onward). Pass num_frames explicitly.";

// Upstream does NOT raise for (AutoDuration, predictor) or (48, None).
// Both were executed at generation time; a guard that always raised
// would have failed this script, not this test.
inline constexpr bool kLtx2RequireNumFramesAllowsExplicitWithoutHead = true;
inline constexpr bool kLtx2RequireNumFramesAllowsAutoWithHead = true;

// AutoDuration's defaults (utils/types.py:116-124), read off the class.
inline constexpr double kLtx2AutoDurationMinSeconds = 1.0;
inline constexpr double kLtx2AutoDurationMaxSeconds = 20.0;

}  // namespace vllm_test
